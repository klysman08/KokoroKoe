# KokoroKoe Project Memory

This checked-in ledger coordinates tasks and handoffs. Do not store secrets, API keys, transcript content, audio, model weights, or personal meeting data here.

## Current phase

- Phase: 4 - Projects and persistence
- Status: In progress
- Started: 2026-08-11
- Objective: Build portable project/session records, Markdown snapshots, recovery journals, and rebuildable SQLite/FTS projections without making SQLite the only copy of important content.

## Completed task table - Phase 1

| ID | Owner | Status | Scope | Dependencies | Acceptance | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| P1-001 | Coordinator | Completed | Initialize, author, and validate the repository development skill and its references | Approved plan | Repository skill initialized, customized, and structurally valid | `init_skill.py`; regenerated `agents/openai.yaml`; `quick_validate.py`: `Skill is valid!` |
| P1-002 | Coordinator | Completed | Create architecture, ADR, dependency/license, privacy/limitations, memory, and repository-instruction artifacts | Approved plan | Phase 1 documents define structure, models, interfaces, strategies, risks, dependencies, and decisions | Required-artifact check passed; local Markdown links resolve; no placeholders/false lockfile claims |
| P1-003 | Coordinator + review agents | Completed | Forward-test normal/security skill behavior and audit Phase 1 documents against the Manifest | P1-001, P1-002 | Independent reviews have no unresolved blocking findings | Phase 2 task forward-test passed; unsafe React/OpenRouter forward-test rejected correctly; independent audit findings fixed; final re-audit: no blockers |
| P1-004 | Coordinator | Completed | Apply findings, run final checks, and record the Phase 1 handoff | P1-003 | Final checks pass; memory and `AGENTS.md` record Phase 1 completion | Skill, artifact, link, placeholder, secret-pattern, whitespace, fence, and Git checks passed on 2026-08-05 |

## Phase 2 task table

| ID | Owner | Status | Scope | Dependencies | Acceptance | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| P2-001 | Coordinator + review agents | Completed | Windows Tauri/React scaffold, strict frontend toolchain, shadcn/Tailwind shell, UI-only state/routing, least-privilege capability, audits, package build, and smoke launch | P1-004; Node 24; MSVC/SDK; CMake; WebView2 | Clean locked install and frontend/Rust checks; explicit main capability with no permissions; x64 bundles and launch smoke pass | Node 24/pnpm 10 checks; 2 Vitest tests; Vite build; Rust fmt/Clippy/test; clean pnpm audit; cargo audit; cargo-deny; MSI/NSIS build; responding-process smoke; two independent read-only reviews |
| P2-002 | Coordinator + review agents | Completed | Sanitized tracing/errors, strict Rust/Zod settings boundary, read-only `get_settings`, TanStack Query integration, root/query error UI, and least-privilege command ACL | P2-001 | Shared fixtures parse in Rust/TypeScript; semantic validation matches; raw errors/tokens/paths do not reach logs or reports; only `main` can invoke the command; builds/audits/package smoke pass | 15 Rust tests; 27 Vitest tests; Node 24 format/lint/type/build; generated ACL/static capability checks; secret/path canaries; cargo-deny/audit and pnpm audit; MSI/NSIS rebuild; responding-process smoke; two independent reviews with blockers fixed and rechecked |
| P2-003 | Coordinator + review agents | Completed | Versioned non-secret SQLite settings persistence, validated workspace selection/write-health probe, optimistic settings updates, strict mutation contracts, onboarding UI, and least-privilege commands | P2-002 | Corrupt-record recovery, traversal/ADS/reparse denial, optimistic-revision conflicts, Rust/Zod fixture parity, denied-window checks, audits, package build, and Windows onboarding smoke pass | 37 Rust tests; 51 Vitest tests; Node/Rust quality gates and audits; MSI/NSIS rebuild; packaged picker/update/restart smoke; three independent reviews with no remaining blockers |
| P2-004 | Coordinator + review agents | Completed | Sanitized raw-HTML-disabled Markdown rendering boundary, locked Windows CI, reproducible verification commands, and Phase 2 acceptance/handoff | P2-003 | Untrusted Markdown cannot execute HTML/script, load images, or navigate through unsafe links; Windows CI covers locked frontend/Rust format, lint, type, unit, capability, audit, and desktop build checks; Phase 2 evidence and limitations are independently reviewed | 55 Vitest tests; 37 Rust/capability tests; Node/Rust format/lint/type/build; clean production audit; license inventory; Cargo deny/audit; actionlint; locked MSI/NSIS build; packaged Settings smoke; three independent reviews with no remaining blockers |

## Phase 3 task table

| ID | Owner | Status | Scope | Dependencies | Acceptance | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| P3-001 | Coordinator | Completed | Bounded Windows WASAPI prototype: active endpoint/default-role enumeration, simultaneous shared-mode event-driven microphone and render-loopback capture, QPC-derived session timestamps, independent channel supervision/retry, bounded packet queues, and native-format/health diagnostics | P2-004; ADR 0002; Windows 10 22H2/11 x64; active capture and render endpoints | Deterministic queue/timeline/supervisor tests pass; an explicit Windows hardware probe captures both sources concurrently without retaining samples, reports monotonic QPC-derived timestamps and native formats, bounds memory with visible drop/discontinuity counters, and demonstrates that one channel failure/retry does not stop the other; dependency, capability, privacy, and scope checks pass | 70 Vitest tests; 45 ordinary Rust tests plus 3 capability tests; explicit dual-source hardware probe passed; zero timestamp regressions or queue drops; locked frontend/Rust quality gates, audits, and x64 release build passed |
| P3-002 | Coordinator | Completed | Bounded Rust audio-processing prototype: validate and decode supported native PCM/float packets, sanitize non-finite samples, downmix each source independently, anti-aliased resample to 16 kHz mono, derive throttled RMS/peak/clipping diagnostics, and pass normalized chunks through bounded per-source queues | P3-001; ADR 0002; `rubato` API/license/MSRV verification; deterministic format fixtures | In-memory fixtures cover unsigned 8-bit and signed 16/24/32-bit PCM including left-aligned valid bits, 32/64-bit float and non-finite input, mono/stereo/multichannel downmix, rate conversion and anti-aliasing, level windows, format changes, queue pressure, malformed packets, and source isolation; an explicit Windows probe processes both live sources without retaining samples; full checks, audits, scope review, and release build pass | 72 Vitest tests; 54 ordinary Rust tests plus 3 capability tests; explicit dual-source normalization probe passed on 44.1/48 kHz stereo-float endpoints with zero processing errors or queue drops; locked Node/Rust checks, audits, and x64 release build passed |
| P3-003 | Coordinator | Completed | Source-local VAD and bounded utterance-segmentation prototype: compare Earshot 1.2.1 against Silero VAD v6 on one generated deterministic in-memory speech/noise/silence corpus, then integrate only a passing winner with pre-roll, trailing-silence finalization, minimum-speech rejection, forced splitting, reset/finish handling, and aggregate diagnostics | P3-002; ADRs 0002/0003; R-003; dependency API/license/MSRV/model-notice review | Before measurement, fix both detector thresholds at 0.5 and require Earshot speech miss rate <=10%, non-speech false-positive rate <=5%, and F1 no more than 2 percentage points below Silero on identical 32 ms evaluation windows; deterministic segmenter tests prove 300 ms pre-roll, 500 ms trailing silence, 160 ms minimum speech, a hard 30 s split, bounded source-local memory, reset/finish behavior, and source isolation; no audio/model artifact, sample payload, VAD event, transcription, persistence, UI, command, or capability is added; full checks, audits, scope review, and release build pass | Earshot passed the first frozen run at 0.40% miss, 1.07% false positives, and 0.9901 F1 versus Silero's 0.6413 F1 across 625 identical 32 ms windows; 60 ordinary Rust tests, 3 capability tests, 72 Vitest tests, the exact comparison and dual-source live VAD probes, locked audits, and x64 release build passed |
| P3-004 | Coordinator | Completed | Bounded CPU-only local Whisper runtime: strict source/timeline/sample/result contracts, one model owner, project-owned C ABI shim over pinned MIT whisper.cpp, Tiny/Base local throughput probe, fixed errors, and process-lifetime native module ownership | P3-003; ADR 0003; R-004/R-006; Windows x64; CMake/MSVC/FFmpeg; user-supplied local MP3 | Before measurement, freeze identical Earshot-finalized utterances, separate model-load timing, CPU-only eight-thread inference, aggregate RTF <1.0 as the hard gate, and nearest-rank final p95 against the two-second target; prove malformed model and source-local inference errors remain bounded/recoverable; commit no audio, transcript, model, DLL, download, UI, command, event, persistence, or external-service path; locked checks, audits, native warnings-as-errors build, and x64 release build pass | On Ryzen 7 3700X/31.9 GiB RAM, seven utterances/86.772 s speech: Tiny load 109 ms, RTF 0.0916, p95 1.831 s; Base load 187 ms, RTF 0.1924, p95 3.637 s; malformed load recovered before both models; 63 ordinary Rust tests plus 3 capability tests and 72 Vitest tests passed; locked audits and x64 release build passed |
| P3-005 | Coordinator | Completed | Explicit Vulkan backend attestation, bounded finalized-utterance CPU fallback, and crash-class driver isolation decision using the P3-004 engine contract and a locally generated speech fixture | P3-004; ADR 0003; R-005; Windows x64; pinned whisper.cpp; Vulkan SDK/runtime; Vulkan-capable and forced-missing-driver probes | Before measurement, require an attested Vulkan model/inference success; force a missing-driver startup failure and a native inference abort in isolated child processes; in each failure scenario deliver the same source/timeline/sample request exactly once through CPU with fixed path-free diagnostics; prove the parent remains alive and choose in-process or supervised-worker acceleration accordingly; add no product command, capability, UI, model catalog/download, live event, partial scheduler/backpressure, retained audio, persistence, or external-service path; full checks, audits, native warnings-as-errors build, and x64 release build pass | Attested Vulkan completed one generated 4.596 s utterance; forced missing-driver startup and native inference abort stayed child-local; each failure produced exactly one CPU result with zero duplicates/losses and selected a supervised worker; 66 ordinary Rust tests plus 3 capability tests and 72 Vitest tests passed; CPU regression, audits, policy, and locked x64 release build passed |
| P3-006 | Coordinator | Completed | Pure Rust prioritized transcription-scheduler and backpressure prototype with owned bounded jobs, chronological finals across both sources, one replaceable partial per source, partial-work hysteresis, explicit final-backlog gaps, deterministic slowed inference, and a two-simulated-hour soak | P3-005; ADR 0003; finalized-utterance/sample bounds; stable `microphone`/`system_output` identifiers | Before the soak, fix final priority, pending-final chronological ordering, fair replaceable partial selection, partial disable at >=20 s and restore below 10 s of queued final speech, a 600 s/4,096-job final backlog ceiling, and a fixed path-free gap record for every rejected final; use deterministic 5x fake inference over alternating one-second source jobs for two simulated hours; prove both sources make progress, no accepted final is duplicated or reordered, gaps are explicit, partial storage never exceeds two jobs, and logical queued sample memory plateaus within the frozen bounds; add no live capture wiring, Whisper/model load, Vulkan worker protocol, Tauri command/capability/event, UI, persistence, retained audio, or external service | Two-hour/5x soak admitted 2,040 finals, started 1,440 (720/source), returned 5,160 explicit balanced gaps, and held the second-hour logical backlog exactly at 9,600,000 samples/600 jobs; 8 focused scheduler tests, 74 ordinary Rust tests plus 3 capability tests, 72 Vitest tests, audits, policy, and locked x64 release build passed |
| P3-007 | Coordinator | Completed | Long-run QPC alignment and source-local Windows capture-recovery gate with durable aggregate failure/retry/gap diagnostics | P3-001/P3-002; ADR 0002; R-001/R-002; active default Windows capture/render endpoints | Before measurement, fix a deterministic two-hour simulation using one large QPC epoch, an awkward 10,000,003 Hz counter, 44.1 kHz/441-frame microphone packets, 48 kHz/512-frame system packets, and one 250 ms source-local discontinuity; require each source's calculated timeline error below 20 ms, monotonic timestamps, and no epoch rebase or gap compression. Then run an ignored bounded live dual-endpoint probe with one test-only injected capture-loop failure; require the affected source to retry and recover, the unaffected source to advance throughout recovery, and durable aggregate diagnostics to expose every failure and completed recovery gap after the transient error clears. Add no product command/event/UI, transcription/model wiring, persistence, retained audio, capability, external service, or production fault-injection surface. | Two-hour simulation covered 720,001 microphone and 675,001 system timestamps with 0 ms maximum calculated error and zero regressions; the 250 ms discontinuity remained a 260 ms jump. Two live runs restarted microphone on attempt 2, retained one 264-265 ms recovery gap and its fixed failure code, advanced system output by 51-53 packets, and resumed microphone for 24-26 packets; full frontend/Rust gates, audits, policy, and locked x64 release build passed. |
| P3-008 | Coordinator | Completed | Versioned bounded local IPC and Windows lifecycle supervision for the ADR 0007 Vulkan transcription worker, with lazy exact CPU recovery | P3-004/P3-005/P3-006; ADRs 0003/0007; pinned adapter API v2; external Tiny model/generated fixture; Windows x64 | Before measurement, freeze protocol v1 as strict length-prefixed JSON frames capped at 16 MiB, carrying at most one validated 480,000-sample finalized utterance; require attested Vulkan before work, one mutable in-flight request, a 30 s startup ceiling, 5 s frame-write ceiling, 60 s inference/read ceiling, 10 ms cancellation polling, and Windows Job Object kill-on-close cleanup. The exact external probe must prove healthy Vulkan completion plus missing-driver startup failure, malformed hello/response rejection, blocked-write timeout, hung/crashed inference isolation, explicit cancellation, descendant cleanup, and lazy CPU load only after worker termination; each non-cancelled failed accelerated attempt must deliver exactly one CPU result with zero duplicate/lost finals. Add no model catalog/download, live audio/scheduler wiring, Tauri command/event/capability, UI, persistence, retained audio, OpenRouter, packaged worker claim, or release fault-injection surface. | Protocol v1 accepted one attested Vulkan result; seven startup/protocol/write/inference failure attempts each returned exactly one CPU result after worker termination; cancellation returned none; duplicate/lost results were zero; a confirmed descendant was removed; 81 ordinary Rust tests, 3 capability tests, 72 Vitest tests, audits, policy, and the locked x64 release build passed |
| P3-009 | Coordinator | Completed | Bounded Rust-only integration of source-local finalized VAD output, cross-source ordering watermarks, the prioritized scheduler, and the supervised worker/CPU inference owner | P3-002/P3-003/P3-006/P3-008; ADRs 0002/0003/0007; stable source/timeline/result contracts | Freeze each open VAD source's ordering frontier as the earliest start time any future final could still have; a queued final may start only when its start is strictly below both open frontiers, while a sealed source contributes infinity. Preserve scheduler tie order by start/end/arrival sequence. Pause and stop must first accept each source's terminal VAD flush, seal both sources, discard provisional work, and account for every accepted final as exactly one result or fixed gap before reaching their boundary state; resume starts with empty source frontiers on the same nondecreasing session timeline. Explicit cancellation must never trigger CPU recovery and must account for cancelled work as fixed gaps. Deterministic delayed-source and 5x-pressure gates must prove chronological exact-once finals, balanced source progress, explicit hard-cap/error/cancellation gaps, bounded scheduler/VAD memory, source isolation, and one-shot CPU recovery after an accelerated failure. Add no hardware capture run, model catalog/download, frontend command/event/capability/UI, Markdown/SQLite persistence, retained audio, OpenRouter, or packaging claim. | Two simulated hours received 7,200 finals, delivered 2,039 chronological results (1,020 microphone/1,019 system output), returned 5,161 explicit gaps, and peaked at exactly 600 jobs/600,000 ms/9,600,000 samples; all IDs were unique and exactly accounted. Delayed-source ordering, VAD pre-roll frontiers, pause/resume/stop, cancellation with zero CPU loads, source failure isolation, one-shot lazy CPU recovery, 90 ordinary Rust tests, 3 capability tests, 72 Vitest tests, audits, policy, and locked x64 release build passed |
| P3-010 | Coordinator | Completed | Rust-only curated multilingual Whisper Tiny/Base catalog, compatibility diagnostics, resumable verified installation, ephemeral selection, deletion, and corrupt/interrupted-download recovery | P3-004/P3-008/P3-009; ADRs 0003/0007; app-local model root; pinned upstream model evidence; reqwest/Rustls/SHA-256 dependency review | Pin immutable source revision, URL, exact bytes, SHA-256, MIT provenance, language, memory guidance, and CPU/Vulkan backends; keep every URL/path Rust/catalog-owned; require strict range semantics, bounded cancellation, full verification, synced same-directory rename, disk preflight, visible memory warning, verified-only selection, and selected-model deletion denial; deterministic recovery tests and exact external Tiny/Base hashes pass without adding command/event/UI/persistence/audio/OpenRouter/packaging scope | 12 focused ordinary tests passed; exact external gate rehashed 77,691,713-byte Tiny and 147,951,465-byte Base; full Rust gate passed 102 ordinary plus 3 capability tests; Node 24 frontend gate passed 72 Vitest tests; Cargo policy/audit and npm audit/licenses passed; locked x64 release build passed |
| P3-011 | Primary Codex agent | Completed | Product integration of curated model listing/download/resume/cancel/delete/default selection through strict Rust/Zod contracts, SQLite operational state, background work, main-window ACL, progress events, and Settings UI | P3-010; P2 settings/query/error/SQLite/capability patterns; accepted model command/event architecture; Tauri 2.11.5 event API; Rust `time` RFC 3339 support | Freeze six exact model commands and one main-targeted versioned progress event; authorize before state/network/filesystem side effects; keep URL/path/hash ownership in Rust; persist bounded non-secret job/install/default state with interrupted running jobs recovered as paused; run blocking HTTP/hash/filesystem work outside the UI thread; validate default selection against a verified install and reject generic settings mutation of that field; prove strict shared contracts, progress ordering, cancellation/resume, restart recovery, optimistic revision conflicts, deletion safety, UI actions, ACL/capability minimality, audits, and x64 build without live-session/native-package/retained-audio/OpenRouter scope | Shared Rust/Zod fixture; 109 ordinary Rust tests plus 3 capability tests; 77 Vitest tests; locked format/lint/type/Clippy/build/policy gates; clean production npm audit and Cargo policy/audit; bounded release launch health smoke; native visual automation unavailable and recorded below |
| P3-012 | Primary Codex agent | Completed | Product audio-device enumeration and independent source-local microphone/system-output tests through the three exact architecture commands, bounded main-window level/health events, transient default-role/fixed-endpoint selection controls, and Settings UI | P3-001/P3-002/P3-007; ADR 0002; strict command/error/event and exact-main capability patterns; active Windows capture/render endpoints | Replace prototype command exposure with `list_audio_devices`, `start_audio_device_test`, and `stop_audio_device_test`; validate strict shared Rust/Zod contracts and source/direction/request identity; permit at most one test per source while allowing both sources independently; capture/process/discard samples only in bounded Rust queues; target throttled aggregate level and health events only to `main`; follow default-role changes but never silently switch fixed endpoints; prove stop/idempotency/error isolation, UI selection/actions, ACL minimality, hardware behavior, full gates, audits, and x64 build without session/transcript/audio-retention/model-runtime/OpenRouter scope | Shared Rust/Zod fixture; 111 ordinary Rust tests plus 3 capability tests and one explicit product hardware test; 76 Vitest tests; locked frontend/Rust gates, clean npm audit, Cargo policy/audit with 17 existing allowed warnings, x64 release build, and launch smoke |
| P3-013 | Primary Codex agent | Completed | Rust-only live-transcription readiness integration from independent Windows capture/process streams and verified default-model resolution through the P3-009 coordinator to a bounded in-memory sink | P3-009/P3-011/P3-012; ADRs 0002/0003/0007; external native adapter for the explicit gate; active microphone/render endpoints | Resolve only the verified selected Tiny/Base artifact inside Rust; hand finalized source-local VAD outcomes through bounded queues without blocking capture callbacks; preserve cross-source chronological exact-once final accounting as result or fixed gap; prove source isolation, cancellation without fallback, and bounded stop after terminal VAD flush; add an ignored live dual-endpoint/installed-model gate using `public/audio_test/OpenAI.mp3`; add no product command/event/UI, persistence, retained audio, OpenRouter, generic permission, or packaging claim | Five focused handoff/coordinator tests; verified-default resolver test; live Tiny/CPU gate with 2,394 microphone and 2,398 system packets, one non-empty final, and zero gaps; 117 ordinary Rust tests plus 3 capability tests; 76 Vitest tests; locked gates, audits, x64 release build, and launch smoke |
| P3-014 | Primary Codex agent | Completed | Rust-only bounded partial-transcription readiness from source-local active VAD snapshots through the existing replaceable scheduler slots and P3-013 live coordinator | P3-003/P3-006/P3-009/P3-013; ADR 0003; stable sources; verified selected Tiny/Base runtime | Snapshot only active VAD speech after the accepted minimum, at a fixed low-latency cadence and within the 30 s sample ceiling; retain at most one replaceable partial per source; preserve stable partial-to-final identity; keep eligible finals ahead of partials and inherit 20 s/10 s hysteresis; discard provisional work on terminal/cancel/error without creating false final gaps; prove identity, replacement, isolation, bounds, precedence, cleanup, and a selected-model fixture gate without product commands/events/UI/persistence/retained audio/OpenRouter/packaging scope | 120 ordinary Rust tests plus 3 capability tests; selected-Tiny live gate with 2,394 microphone packets, 2,399 system packets, one non-empty partial, one matching final, and zero gaps; 76 Vitest tests; locked gates, audits, x64 release build, and launch smoke |
| P3-015 | Primary Codex agent | Completed | Product-facing transient live-transcription run through exact main-window commands, ordered partial/final/gap events, and a bounded real-time transcript UI | P3-011/P3-012/P3-013/P3-014; ADRs 0002/0003/0007; exact-main authorization; verified selected model | Freeze a non-persistent live-run boundary distinct from Phase 4 sessions; authorize before model/audio state; require explicit capture consent; use configured verified model plus two source selections; emit strict source-labelled ordered events only to `main`; reconcile stable partials to finals; cap UI memory at 500 records; stop/drain idempotently; prove contracts, ACL, lifecycle, malformed-event rejection, replacement, bounds, and explicit Windows fixture behavior without persistence/retained audio/OpenRouter/extra windows/native packaging | Shared Rust/Zod fixture; 126 ordinary Rust tests plus 3 capability tests and one explicit product hardware gate; 83 Vitest tests; `OpenAI.mp3` produced 45 product partial updates, one matching final, and zero gaps; locked gates, audits, x64 release build, and launch smoke passed |

## Phase 4 task table

| ID | Owner | Status | Scope | Dependencies | Acceptance | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| P4-001 | Primary Codex agent | Completed | Strict project/session domain records, frozen preset/device snapshots, deterministic portable folder names, and a fixed relative Markdown/journal/audio layout contract | Phase 3 completion audit; ADRs 0001/0005/0006; accepted architecture domain/layout; strict Rust/Zod fixture patterns | Shared Rust/Zod fixture rejects malformed, unknown, nullable, unsafe, and cross-record-inconsistent values; persisted folder names bind to UUID suffixes and session date; derived paths are exact relative descendants with no caller path input; add no filesystem write, SQLite project/session row, command, capability, UI, live-session wiring, retained audio, OpenRouter, or native packaging | Shared fixture and adversarial tests; 131 ordinary Rust tests plus 3 capability tests; 98 Vitest tests; locked gates, clean npm audit, Cargo policy/audit with 17 existing allowed warnings, x64 release build, responsive launch smoke, and final diff/artifact checks |
| P4-002 | Primary Codex agent | Completed | Rust-owned pinned workspace/project creation and strict `project.md` YAML-front-matter snapshot publication | P4-001; ADRs 0001/0005; accepted persistence ownership/layout; existing Windows workspace policy and `windows-sys` APIs | Pin/revalidate workspace and derived directory identities with no-delete-share handles; reject reparse-backed paths; create only derived project artifacts; quote untrusted fields without YAML shape escape; sync and atomically replace a same-directory temporary file; reject duplicates without overwrite; clean only attempt-owned artifacts at every injected fault; add no read/update/session/journal/SQLite/command/capability/UI/audio/OpenRouter/packaging scope | Golden Markdown fixture; hostile-scalar, identity-pin, junction, duplicate, exact-layout, and three-stage fault tests; 139 ordinary Rust tests plus 3 capability tests; 98 Vitest tests; locked gates, clean npm audit, Cargo policy/audit with 17 existing allowed warnings, x64 release build, responsive launch smoke, and final diff/artifact checks |
| P4-003 | Primary Codex agent | Completed | Bounded strict project snapshot reading, optimistic updates, external-edit detection, atomic `.bak` retention, and recovery | P4-002; ADRs 0001/0005/0006; canonical Project/YAML contract; existing SHA-256 and Windows filesystem APIs | Cap reads at 128 KiB; reject noncanonical/unsafe front matter and identity mismatch; return exact-byte fingerprint; require expected revision/fingerprint and immutable identity; lock against concurrent writes through replacement; retain previous valid snapshot as `.bak`; recover missing/malformed/oversized primary without clobbering a new external file; preserve unrecoverable input; add no discovery/SQLite/command/capability/UI/session/journal/audio/OpenRouter/packaging scope | 16 focused parser/update/conflict/recovery/fault tests; 147 ordinary Rust tests plus 3 capability tests; 98 Vitest tests; locked gates, clean npm audit, Cargo policy/audit with 17 existing allowed warnings, x64 release build, responsive launch smoke, and final scope/diff checks |
| P4-004 | Primary Codex agent | Completed | Bounded direct-child project discovery and an app-local rebuildable SQLite projection with stable ordering and generation-bound pagination | P4-003; Markdown source-of-truth policy; pinned project store; rusqlite recovery boundary | Validate every accepted project through the pinned store; bound discovery/issues/pages; exclude duplicate identities; rebuild atomically from Markdown; quarantine physical SQLite corruption; invalidate stale cursors; add no command/UI/session/network scope | 24 focused tests; 155 ordinary Rust tests plus 3 capability tests; 98 Vitest tests; locked gates, audits, x64 release build, and launch smoke |
| P4-005 | Primary Codex agent | Completed | Exact-main project list/get/create/update product boundary with strict shared contracts, transactional projection refresh, workspace-bound indexing, and bounded Home UI | P4-004; P4-003 revision/fingerprint conflict semantics; settings workspace serialization; exact-main authorization | Validate requests/responses in Rust and React; authorize before state access; keep filesystem work in blocking Rust; serialize workspace/project changes; invalidate projection before Markdown mutation and acknowledge only after rebuild; repair pending projection on next read; bind index to an opaque workspace identity; list 24 projects per UI page and support create/versioned edit without direct filesystem access | 36 focused Rust project tests; 163 ordinary Rust tests plus 3 capability tests; 116 Vitest tests; locked Node 24/Rust gates, clean audits, x64 release build, and responsive launch smoke |

## Confirmed decisions

| Date | Decision | Rationale |
| --- | --- | --- |
| 2026-08-05 | Target Windows 10 22H2 and Windows 11 x64 | Fixed MVP boundary; ARM64 and other OSes remain future adapters |
| 2026-08-05 | Keep the repository skill in `.agents/skills/kokorokoe-development` | Shareable, versioned workflow for contributors and sub-agents |
| 2026-08-05 | Use this checked-in memory rather than user-local generated memory | Auditable coordination and handoff record |
| 2026-08-05 | Keep Rust as the sensitive-operation trust boundary | Prevent React from accessing audio, arbitrary files, credentials, models, or external APIs |
| 2026-08-05 | Use independent shared-mode WASAPI microphone and render-loopback capture | Required Windows device control, loopback, recovery, and timestamps |
| 2026-08-05 | Use QPC packet timestamps for one session timeline | Align independent endpoints without callback-arrival assumptions |
| 2026-08-05 | Keep Markdown as portable truth and SQLite as a rebuildable projection | Human-readable export plus fast local loading/search |
| 2026-08-05 | Start with multilingual Whisper Tiny and Base; Base is the provisional default | Keep Base as default only if the Phase 3 minimum-hardware throughput gate passes; otherwise select Tiny |
| 2026-08-05 | Use one prioritized model-owning inference worker | Limit memory while protecting final transcription from partial work |
| 2026-08-05 | Keep LLM and audio retention disabled by default | Privacy-first behavior and explicit user control |
| 2026-08-05 | Use Windows Credential Manager for the OpenRouter key | Meets the OS-backed secret-storage requirement |
| 2026-08-05 | Use FTS5 plus recency for MVP retrieval | Avoid a second local embedding model while limiting transcript resend |
| 2026-08-05 | Use shadcn preset `b0` with the current Vite/Tailwind v4 workflow | The official CLI decoded `b0` successfully and generated the selected Nova/Base UI foundation |
| 2026-08-05 | Keep P2-001's main-window capability at zero permissions | The static shell needs no commands, plugins, filesystem, network, process, or credential access |
| 2026-08-05 | Use a typed local hash-navigation store for the two foundation routes | Removes React Router while its available releases are covered by unresolved advisories; revisit only when a patched version is published and needed |
| 2026-08-05 | Keep TanStack Query and Zod out of P2-001 | No asynchronous command/cache or external contract exists yet; add them with the first typed Rust command boundary where they provide value |
| 2026-08-05 | Install Graphify globally as a Codex skill | User-requested local codebase graph tooling; installed through the official `graphifyy` CLI at version 0.9.33 |
| 2026-08-05 | Serialize command failures as `{ error: AppError }` | Freezes one strict Rust/Zod rejection envelope without exposing raw exceptions or provider/source errors |
| 2026-08-05 | Restrict `get_settings` through both Tauri ACL and an exact Rust `main` window-label check | Tauri application commands are otherwise available to local windows by default; defense in depth prevents future windows from inheriting access accidentally |
| 2026-08-05 | Keep foundation settings read-only and query-cached only while the Settings view is mounted | P2-002 validates the asynchronous boundary without inventing persistence, copying settings into Zustand, or retaining the workspace path in a persistent cache |
| 2026-08-05 | Use fixed crate-filtered stderr tracing with safe categorical fields only | Prevent dependency/payload tracing and raw source details from entering logs; file logging remains unimplemented |
| 2026-08-08 | Store non-secret settings as an append-only SQLite history retaining three validated revisions | Optimistic mutations, monotonic recovery revisions, and fallback from semantically invalid records avoid stale-write ABA behavior |
| 2026-08-08 | Lazily initialize and access SQLite only in blocking workers | Tauri setup remains responsive and can show sanitized command errors when storage is corrupt, busy, inaccessible, or from a future schema |
| 2026-08-08 | Quarantine and rebuild the non-secret settings database only for SQLite corruption/not-a-database failures | Preserve damaged files for diagnosis while keeping future-schema, busy, permission, and ordinary I/O failures fail-closed without destructive recovery |
| 2026-08-08 | Restrict Windows MVP workspaces to existing fixed local drive-letter directories without reparse points | Establish a conservative Rust-owned boundary; ADR 0005 records the deliberate rejection of network, removable, redirected, and reparse-backed folders |
| 2026-08-08 | Keep workspace selection in a single-flight Rust native picker with no frontend path or dialog capability | React cannot submit an arbitrary path, and cancellation or concurrent selection leaves settings unchanged |
| 2026-08-08 | Route all future Markdown-derived React UI through the fixed `SanitizedMarkdown` boundary | Raw HTML, images, unsafe schemes, and active navigation remain unavailable; ADR 0006 records the policy and Phase 4 input-size responsibility |
| 2026-08-08 | Use one read-only, full-SHA-pinned Windows 2022 CI workflow for the Phase 2 gates | Match the Windows/MSVC product boundary without adding secrets, publishing, signing, or non-Windows claims; local MSI/NSIS smoke remains separate evidence |
| 2026-08-08 | Use `wasapi` 0.23 with separate bounded Crossbeam packet queues for the Phase 3 capture prototype | Keep unsafe Core Audio details behind a maintained Rust wrapper while ensuring neither source blocks the other or grows memory without bound |
| 2026-08-08 | Re-resolve default-role endpoints during capture while fixed selections retry only their stored endpoint ID | Implements ADR 0002 without silently changing a user's explicit selection |
| 2026-08-08 | Normalize each source in its own Rust worker through `rubato` 4.0 asynchronous sinc resampling and a separate bounded 10 ms output queue | Keeps sample data and compute outside React, preserves source isolation, provides anti-aliasing, and makes downstream backpressure visible without blocking capture |
| 2026-08-08 | Use equal-weight downmix and 100 ms aggregate level windows for P3-002 | Establishes a deterministic bounded baseline while leaving speaker-mask weighting and product level events as explicit future decisions |
| 2026-08-09 | Use Earshot 1.2.1 at threshold 0.5 for source-local VAD with 300 ms pre-roll, 500 ms trailing silence, 160 ms minimum speech, and a 30 s hard split | Earshot passed all pre-recorded generated-corpus gates; it is pure Rust with no runtime model/DLL, while Silero remains a development-only comparator and broader real-speech validation stays open |
| 2026-08-09 | Use a project-owned bounded C ABI shim over pinned MIT whisper.cpp v1.9.2 and reject `whisper-rs` for P3 | Resolves R-006 without accepting the Unlicense wrapper; Rust retains paths, samples, labels, fixed errors, and result bounds |
| 2026-08-09 | Keep the native Whisper/GGML DLL set loaded for process lifetime while explicitly freeing model contexts and results | whisper.cpp dynamically registers CPU backends and faulted on unload/reload; one shared module plus one model owner keeps the native boundary stable and bounded |
| 2026-08-09 | Keep Base as the provisional default after the current CPU RTF gate, while retaining Tiny as the lower-latency option | Both models passed RTF <1.0; Tiny met the two-second p95 target and Base did not, so the shipped default still requires minimum-hardware evidence |
| 2026-08-10 | Keep model downloads single-flight in a blocking Rust service, retain at most 64 strict SQLite job snapshots, and target throttled progress only to `main` | Bounds disk/memory/job state, preserves resumable recovery, prevents React from owning network or paths, and keeps failed event delivery from affecting verified installation |
| 2026-08-09 | Require explicit backend attestation and run Vulkan model loading/inference in a supervised worker | Upstream may silently use CPU when GPU is requested, while a native abort cannot be caught in process; P3-005 proved child-local failure and exact CPU recovery, recorded in ADR 0007 |
| 2026-08-09 | Use the KokoroKoe executable's private worker mode with bounded anonymous pipes and a kill-on-close Windows Job Object | Keeps Vulkan outside the Tauri process without adding a frontend process capability; protocol v1 has one bounded in-flight utterance, strict validation/timeouts, descendant cleanup, and lazy CPU recovery only after worker termination |
| 2026-08-09 | Load the CPU model only after the Vulkan worker fails or is unavailable | Worker termination releases the accelerated model before parent CPU load, preserving the one-heavy-model rule while keeping CPU mandatory |
| 2026-08-09 | Pin the initial Tiny/Base catalog to one immutable upstream revision and install only after exact length/SHA-256 verification | A fixed Rust-owned catalog prevents caller-controlled URLs/paths and ensures resumable or corrupted bytes never become selectable model files |
| 2026-08-09 | Bound pending finals by 600 seconds, 9,600,000 samples, and 4,096 jobs; reject the newest excess final with an explicit gap | Protect accepted chronological finals and bound both sample storage and minimum-duration queue-node overhead; partial work uses 20-second/10-second hysteresis and never delays a pending final |
| 2026-08-09 | Retain source-local capture failure and recovery history as bounded aggregate diagnostics after a stream becomes active again | A cleared transient error must not erase retry/gap evidence; counters, cumulative/last duration, pending-gap start, and the last fixed failure code remain path-free and sample-free |
| 2026-08-09 | Gate cross-source final inference with the minimum open VAD ordering frontier | Queue sorting cannot prevent a delayed source from finalizing older audio after newer inference starts; the frontier includes active speech, pending frames, and retained pre-roll, while a terminally flushed source contributes infinity |

## Prototype and risk register

| ID | Gate or risk | Resolution rule | Target phase | Status |
| --- | --- | --- | --- | --- |
| R-001 | WASAPI device/format/recovery matrix | Unaffected channel continues; all gaps and retries are visible | 3 | Two P3-007 live-injected recovery runs passed on one default pair: microphone restarted on attempt 2, the unaffected source advanced, and one failure/264-265 ms gap remained visible. Real device/default/Audio-service and broader format/driver matrix remain open |
| R-002 | QPC clock alignment and long-run drift | Less than 20 ms calculated error over two hours, excluding physical latency | 3 | Resolved by P3-007's deterministic two-hour shared-epoch gate: 0 ms maximum calculated mapping error and zero regressions across both cadences with an uncompressed source-local discontinuity; real oscillator/acoustic latency is outside this calculated gate |
| R-003 | Earshot versus Silero VAD | Keep Earshot only if it meets the approved miss/false-positive thresholds and remains within two points of Silero | 3 | Frozen generated-corpus gate passed: Earshot miss 0.40%, false positives 1.07%, F1 0.9901 versus Silero 0.6413; broader consented/licensed real-speech and noise matrix remains open |
| R-004 | Tiny/Base real-time throughput | Aggregate real-time factor below 1.0 on a supported configuration; target final p95 below two seconds | 3 | Current Ryzen 7 3700X CPU configuration passed: Tiny RTF 0.0916/p95 1.831 s; Base RTF 0.1924/p95 3.637 s. Minimum-hardware rerun and final shipped-default freeze remain open |
| R-005 | Vulkan failure isolation | Use in-process fallback only if missing/broken drivers cannot prevent startup; otherwise use a supervised worker | 3 | Resolved by P3-005/ADR 0007: attested Vulkan succeeds in a child; missing-driver startup and native inference abort remain isolated; each retries once on CPU with zero duplicate/lost results; supervised worker required |
| R-006 | `whisper-rs` Unlicense review | Record approval or use a minimal MIT `whisper.cpp` C API adapter | 3 | Resolved by P3-004: no `whisper-rs`; project-owned bounded C ABI shim over pinned MIT whisper.cpp v1.9.2 |
| R-007 | Journal and atomic replacement | Fault injection must preserve every acknowledged final segment | 4 | Pending |
| R-008 | Credential Manager and secret containment | Development and packaged canary tests find no secret outside the OS store | 5 | Pending |
| R-009 | OpenRouter privacy/cost/error behavior | Mock and controlled tests cover ZDR, typed errors, SSE, cancellation, and budgets | 5 | Pending |
| R-010 | Transparent/click-through windows | Preserve readable text; omit click-through if emergency recovery is unreliable | 6 | Pending |
| R-011 | Transcription scheduling under sustained overload | Final chronology and source progress survive two simulated hours; partials shed first; logical memory plateaus; every hard-cap rejection is an explicit gap | 3 | Resolved by P3-006 and integrated by P3-009: delayed source frontiers plus 5x service pressure held exactly 600 jobs/9,600,000 samples, delivered 2,039 chronological balanced results, and returned 5,161 explicit gaps with exact unique-ID accounting |

## Completed checkpoints

### Phase 1 - Architecture and coordination

- Completed: 2026-08-05
- Deliverables: repository skill and metadata, phase/verification references, root instructions, architecture and textual component/data-flow design, source tree, core data and Tauri contracts, four ADRs, dependency/license assessment, privacy/consent/limitations record, prototype gates, and this task ledger.
- Skill forward-test: produced a bounded Phase 2 scaffold task with correct dependencies and exclusions.
- Security forward-test: rejected a direct React/localStorage/OpenRouter implementation and preserved the Rust/Credential Manager boundary.
- Independent audit: initially identified missing source tree, incomplete contracts, false lockfile wording, task schema mismatch, provisional-default inconsistency, and credential wording. All findings were corrected; final audit reported no Phase 1 blockers.
- Verification:
  - `python C:\Users\klysm\.codex\skills\.system\skill-creator\scripts\quick_validate.py .agents\skills\kokorokoe-development`
  - required Phase 1 artifact existence check
  - local Markdown link resolution check
  - placeholder/false lockfile claim scan
  - API-key/bearer-token pattern scan
  - trailing-whitespace and balanced-code-fence checks
  - `git diff --check`
  - `git status --short --branch`
- Result: all checks passed. Files remain intentionally uncommitted and unstaged for user review.
- Known limitations: this phase contains documentation and coordination assets only; no Tauri application exists yet. Native MSVC/CMake prerequisites and Node 24 pinning remain Phase 2 prerequisites. Every audio, model, persistence, OpenRouter, credential, window, performance, and packaging prototype gate remains pending in its assigned phase.

### P2-001 - Windows Tauri/React application scaffold

- Completed: 2026-08-05
- Deliverables: Tauri 2.11 Windows x64 application, React 19/strict TypeScript/Vite foundation, Node 24 and pnpm 10 pins, Tailwind CSS v4 with shadcn preset `b0`, generated Button/Card/Badge/Separator components, branded Home and Settings placeholder routes, UI-only Zustand navigation state, restricted CSP, zero-permission main capability, custom Windows icon, ESLint/Prettier/Vitest configuration, Rust and pnpm lockfiles, Cargo deny policy, and development README.
- Prerequisites installed or verified: `fnm` 1.39.0 with Node 24.19.0, Visual Studio 2022 Build Tools 17.14 VCTools workload, Windows SDK 10.0.26100.0, CMake 4.4.1, Rust/Cargo 1.88.0 MSVC target, pnpm 10.30.2, and WebView2 Runtime 151.0.4129.59.
- Official initialization evidence: `create-tauri-app` 4.6.2 with React TypeScript/Tauri 2; Tailwind v4 Vite plugin; `shadcn preset decode b0`; `shadcn init --preset b0 --template vite`; shadcn 4.16.1.
- Verification:
  - Node 24.19.0: `pnpm install --frozen-lockfile`, `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (2 passed), `pnpm build`, and `pnpm audit --prod --audit-level moderate` all passed.
  - Rust 1.88.0: `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test --all-targets --all-features` (1 passed) all passed.
  - `cargo deny --manifest-path src-tauri/Cargo.toml check licenses bans sources` passed for `x86_64-pc-windows-msvc`.
  - `cargo audit --file src-tauri/Cargo.lock` returned success with no vulnerability failure; it reports unmaintained transitive crates and one non-Windows GTK `glib` unsoundness warning documented in `docs/dependency-licenses.md`.
  - `pnpm tauri build --target x86_64-pc-windows-msvc` produced MSI and NSIS bundles; the release executable remained alive and responsive for the five-second launch smoke test.
  - Independent frontend review passed under Node 24; independent security review found no generic frontend filesystem, HTTP, shell, process, credential, Tauri bridge, browser storage, or external navigation access.
- Process note: the generator's `--force` option removed clean tracked planning files despite its narrow description. Git status detected the deletion immediately and the unchanged files were restored from `HEAD` before work continued. Future scaffolds must run in a staging directory and merge intentionally.
- Known limitations: P2-001 does not implement settings persistence, sanitized logging, error boundaries/reports, Zod/Tauri contracts, TanStack Query, Markdown rendering, CI, audio, transcription, projects, sessions, persistence, LLMs, or advanced windows. The application icon is a foundation asset, not an approved final brand. The built MSI/NSIS files are ignored verification artifacts, not release candidates.

### P2-002 - Typed settings and sanitized error boundary

- Completed: 2026-08-05
- Deliverables: Rust `AppSettings`, `AppError`, and `{ error: AppError }` command contracts; exact semantic Rust deserialization checks matching strict Zod schemas; synthetic shared settings/error fixtures; read-only `get_settings`; Tauri app-command manifest and generated permission; exact `main` capability plus Rust-side window authorization; fixed-filter sanitized tracing; React 19 root error callbacks; root and query error surfaces with explicit sanitized-report copying; TanStack Query used only for the settings command; and a read-only privacy-default preview that does not render the machine-specific workspace path.
- Privacy defaults verified: LLM analysis and audio retention are off; zero-data-retention providers are required; data-collecting providers are denied. No settings persistence, filesystem mutation, HTTP, credential, audio, project/session, or additional-window behavior was added.
- Graphify evidence: Graphify 0.9.33 generated an ignored 262-node/309-edge local code graph and identified the existing settings/shell/Tauri bootstrap change surface. No semantic API key was configured, so `--code-only` was used and checked-in architecture/memory remained the documentation source.
- Verification:
  - Node 24.19.0: `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (27 passed), and `pnpm build` passed.
  - Rust 1.88.0: `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test` passed (12 unit plus 3 capability tests).
  - Shared fixtures are parsed and round-tripped by Rust and strict Zod tests; adversarial cases cover unsafe revisions, empty/bounded strings, invalid UUIDs, zero token limits, malformed fixed decimals, unknown keys, enum casing, and nullable optional detail.
  - Secret/path tests cover Rust technical-detail sanitization, captured tracing output, malformed IPC success/rejection data, React render exceptions, copied reports, and clipboard denial.
  - Tauri tests assert only `main`, only `allow-get-settings`, no remote capability, the generated command mapping, and Rust denial for transcript/insights/unknown labels.
  - `pnpm audit --prod --audit-level moderate` found no known vulnerabilities; production license inventory completed; Cargo deny passed; Cargo audit returned success with the same 17 documented transitive warnings.
  - `pnpm tauri build --target x86_64-pc-windows-msvc` rebuilt MSI and NSIS packages; the release executable remained alive and responsive for the five-second hidden launch smoke.
  - Independent frontend/security reviews found Rust/Zod semantic and React 19 default-console blockers; both were fixed with mirrored validation and explicit root callbacks, then rechecked.
- Known limitations: settings are defaults only and are not persisted or editable; the actual workspace path is returned only to the trusted main window and held in a non-persisted query while mounted; capability denial is unit/static tested rather than invoked from a second real WebView because no second window exists yet; technical-detail redaction is defense in depth rather than permission to pass raw source errors; the packaged smoke does not automate Settings navigation or clipboard interaction; sanitized Markdown rendering and CI are still absent.

### P2-003 - Versioned settings persistence and workspace onboarding

- Completed: 2026-08-08
- Deliverables: lazily opened bundled SQLite settings store; append-only three-revision history; optimistic non-secret settings mutations; monotonic recovery after empty or semantically invalid history; one-shot quarantine/rebuild for physical SQLite corruption; native Rust-only single-flight workspace picker; conservative fixed-volume Windows path validation; ancestor reparse/junction rejection; canonicalization and create/write/sync/delete/free-space probe; strict mirrored Rust/Zod mutation and workspace-status contracts; exact `main`-only command ACL plus Rust authorization; TanStack Query mutation/cache reconciliation; workspace onboarding and privacy-settings UI; explicit confirmation for privacy-default relaxation; recording-law and folder-sync notice; ADR 0005; updated dependency and privacy documentation.
- Graphify evidence: the ignored local graph was refreshed after implementation with Graphify 0.9.33 in code-only mode and contains 582 nodes, 1,072 edges, and 33 communities.
- Verification:
  - Node 24.19.0 and pnpm 10.30.2: `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (9 files, 51 tests), `pnpm build`, and `pnpm audit --prod --audit-level moderate` passed.
  - Rust 1.88.0: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test` passed (34 unit plus 3 capability tests).
  - Persistence tests cover concurrent first migration, future-schema preservation, independent-service revision races, same-value updates, semantic-record recovery, monotonic empty-history recovery, non-database quarantine, and valid-header/settings-table-page corruption quarantine and rebuild.
  - Workspace tests cover relative, drive-relative, root, UNC, verbatim/device, traversal, alternate-data-stream, slash, reserved-name, control-character, non-fixed-volume, and ancestor-junction rejection plus write-probe cleanup.
  - Shared Rust/Zod fixtures and adversarial tests cover strict patches, missing/unsafe revisions, explicit `null`, unknown keys, empty updates, bounded decimals/strings, workspace status, malformed success data, and structured command errors.
  - Capability tests assert only `get_settings`, `update_settings`, and `choose_workspace`; only the exact `main` window; no remote URLs or generic dialog/filesystem/HTTP/shell/process permissions; and Rust authorization occurs before store or picker side effects.
  - `cargo deny --manifest-path src-tauri/Cargo.toml check licenses bans sources` passed. `cargo audit --file src-tauri/Cargo.lock` returned success with the same 17 documented allowed transitive maintenance/non-Windows GTK warnings. Production dependency/license inventory passed.
  - `pnpm tauri build --bundles msi,nsis` rebuilt the x64 MSI and NSIS packages from the final source.
  - Packaged Windows UI smoke opened Settings, verified the default Documents workspace, cancelled the native picker without mutation, selected the valid default folder and observed revision/free-space status, changed the token limit from 2048 to 2049, restarted the executable and observed revision 3/value 2049, restored 2048 at revision 4, and closed the process cleanly.
  - Independent frontend/contracts, persistence/concurrency, and security reviews reported no remaining blockers after migration-race, future-schema, corruption-routing, workspace validation, optimistic-cache, cancellation, and ACL findings were corrected.
- Known limitations: this database contains only non-secret application settings; important project/session content remains future Markdown work. The fixed-local-volume policy intentionally rejects network, removable, redirected, OneDrive/reparse-backed, and some enterprise folders. A successful probe is point-in-time evidence, so Phase 4 must pin/revalidate directory identity and containment for every sensitive write. Physical settings corruption is quarantined and rebuilt rather than repaired or exposed through a recovery UI. Denied-window behavior is still static/unit tested because no second product WebView exists. Sanitized Markdown rendering and CI are not yet implemented.

### P2-004 - Sanitized Markdown, Windows CI, and Phase 2 closeout

- Completed: 2026-08-08
- Deliverables: fixed `SanitizedMarkdown` React boundary; CommonMark/GFM parsing; raw-HTML suppression; explicit sanitize schema; image, embedded-content, DOM-injection, and unsafe-scheme denial; inert external links; adversarial tests; exact Markdown dependency locks and license record; read-only Windows 2022 GitHub Actions workflow with full commit-SHA pins; locked Node/pnpm/Rust/audit/build commands; repository policy and parsed frontend license scripts; development/CI guide; ADR 0006; architecture and privacy updates; Phase 2 acceptance handoff.
- Security behavior: the renderer accepts an in-memory string and presentation class only. Feature code cannot replace parser plugins, element policy, sanitizer, or URL transform. Raw HTML, script/style/SVG/object/iframe/form/input, event-handler attributes, raw and Markdown images, relative/protocol-relative links, and JavaScript/data/VBScript/file schemes are excluded. Even allowed `http`, `https`, and `mailto` links render as inert text. No Tauri command, capability, filesystem read, external HTTP client, credential access, OpenRouter feature, project/session schema, audio code, model code, or additional window was added.
- Graphify evidence: the ignored code graph was refreshed after implementation and contains 770 nodes, 1,258 edges, and 46 communities. Graphify reported five JSON fixtures with no structural nodes, the existing missing optional SQL parser, and community-label drift after the incremental rebuild; checked-in fixtures and migration tests remain the authoritative evidence for those files, and graph labels are non-product local tooling output.
- Verification:
  - Node 24.19.0 and pnpm 10.30.2: `pnpm install --frozen-lockfile`, `pnpm verify:frontend`, `pnpm audit:frontend`, and `pnpm licenses:frontend` passed. Prettier, ESLint, strict TypeScript, 10 Vitest files/55 tests, Vite production build, repository policy, a clean production audit, and a parsed 159-package production license inventory passed.
  - Renderer tests cover supported headings/emphasis/lists/tables/task-list handling, raw executable and embedded elements, raw/Markdown images, inert navigation, mixed-case JavaScript, VBScript, file, data, protocol-relative, relative, and control-character addresses.
  - Rust 1.88.0: `pnpm verify:rust` passed locked MSVC Clippy with warnings denied and 34 unit plus 3 capability tests. `pnpm audit:rust` passed Cargo licenses, bans, and sources; RustSec returned success with the same 17 documented allowed transitive maintenance/non-Windows GTK warnings.
  - `actionlint` 1.7.12 passed after its release archive matched SHA-256 `cdc8643b2c8dc890c76ad16095da97e75f86572805cc3573cc13f31ea0f19127`. Action tag mappings were independently checked; the annotated pnpm v6.0.9 tag was corrected to peeled commit `0ebf47130e4866e96fce0953f49152a61190b271`.
  - `pnpm tauri build --ci --bundles msi,nsis --target x86_64-pc-windows-msvc -- --locked` rebuilt `KokoroKoe_0.1.0_x64_en-US.msi` and `KokoroKoe_0.1.0_x64-setup.exe` from final source.
  - Packaged Windows smoke launched the release executable, confirmed the process was responsive, opened Settings through UI Automation, observed workspace onboarding and local privacy defaults, and closed cleanly.
  - Frozen-lockfile reinstall, local Markdown-link resolution, ignored/untracked `.env` verification without reading it, secret-pattern scan, `git diff --check`, scope review, and three independent final reviews passed with no remaining blockers.
- Known limitations: the GitHub workflow is checked, actionlint-clean, and locally mirrored but has not been observed on a hosted runner because the branch has not been pushed. The hosted image is mutable, and a cold run under the 60-minute timeout remains unproven. CI builds the locked release executable; the installer build and UI smoke are local closeout gates. Frontend license automation proves a nonempty parsed inventory but does not yet enforce an SPDX allowlist or generate distribution notices. Markdown parsing is synchronous, so Phase 4 must bound and paginate document input before React receives it. Raw HTML, images, active links, relative links, and task-list checkboxes remain deliberately unavailable. No project/session Markdown loading or persistence exists yet.

### Phase 2 - Foundation completion

- Completed: 2026-08-08
- Result: the Windows x64 Tauri/React foundation now has a strict typed shell, shadcn/Tailwind UI, local UI state and routing, sanitized error/log boundaries, least-privilege commands/capabilities, versioned non-secret settings persistence, conservative native workspace onboarding, a sanitized Markdown rendering boundary, reproducible local gates, Windows CI configuration, audits, and verified MSI/NSIS packaging.
- Deferred by design: all WASAPI capture, audio processing, VAD, local transcription, model management/downloads, project/session schemas and Markdown persistence, FTS/search, Credential Manager/OpenRouter, insights/summaries, extra windows, opacity/shortcuts, and release signing/telemetry remain in their assigned later phases.

### P3-001 - Bounded Windows WASAPI capture prototype

- Completed: 2026-08-08
- Deliverables: active input/render endpoint and default-role enumeration; strict Rust/Zod device-selection and diagnostic contracts; four exact main-window Tauri commands; explicit capture-consent acknowledgement; simultaneous event-driven shared-mode microphone and render-loopback threads; native mix-format reporting; one shared QPC-derived millisecond epoch; source-local health, retry, and default-endpoint re-resolution; separate bounded nonblocking packet queues; aggregate drop, discontinuity, timestamp, packet, frame, and consumption counters; no sample persistence or frontend audio delivery; implementation, privacy, dependency, and hardware-probe documentation.
- Verification:
  - Node 24.14.0 and pnpm 10.30.2: `pnpm verify:frontend` passed Prettier, ESLint, strict TypeScript, 12 Vitest files/70 tests, Vite production build, and repository policy; `pnpm audit:frontend` found no known production vulnerability.
  - Rust 1.88.0: `pnpm verify:rust` passed locked MSVC rustfmt, Clippy with warnings denied, 45 ordinary tests, and 3 capability tests; one explicitly hardware-dependent test remains ignored in the ordinary suite.
  - The opt-in hardware probe passed on the current Windows machine with concurrent 44.1 kHz stereo-float microphone and 48 kHz stereo-float render-loopback capture. Both streams used one capture attempt, consumed every captured packet, and reported zero queue drops, timestamp errors, or timestamp regressions. One initial discontinuity per stream remained visible in diagnostics.
  - `pnpm audit:rust` passed Cargo licenses, bans, and sources; RustSec returned success with the same 17 documented allowed transitive maintenance/non-Windows GTK warnings.
  - `pnpm tauri build --ci --no-bundle --target x86_64-pc-windows-msvc -- --locked` produced the final locked release executable.
  - Final source review, scope review, local-link/secret/artifact checks, `git diff --check`, and `git status --short --branch` passed without retained audio, models, databases, logs, or build artifacts entering the worktree.
- Known limitations: the live evidence covers one default microphone/render pair and a short run only. Real unplug/hotplug, fixed-device removal, default switches, Windows Audio restart, Bluetooth, docks, USB, virtual devices, Remote Desktop, protected/exclusive audio, PCM integer and broader channel/sample-rate formats, and the two-hour QPC drift target remain open Phase 3 gates. P3-001 does not normalize, resample, meter, run VAD, retain audio, transcribe, persist meetings, or provide product capture UI.

### P3-002 - Bounded audio-processing prototype

- Completed: 2026-08-08
- Deliverables: strict native-format and packet-size validation; unsigned 8-bit and signed little-endian 16/24/32-bit PCM decoding with left-aligned valid-bit handling; 32/64-bit float decoding with non-finite substitution; finite/clamped equal-weight mono downmix; source-local `rubato` asynchronous sinc pipelines; exact 160-sample 16 kHz output chunks; independent bounded normalized queues with fair discard sink and visible drops; 100 ms RMS/peak/clipping windows; aggregate Rust/Zod status counters; one shared golden status fixture; dependency, privacy, development, and prototype-boundary documentation; no new command, capability, UI, event, persistence, model, or network path.
- Verification:
  - Node 24.19.0 and pnpm 10.30.2: `pnpm verify:frontend` passed Prettier, ESLint, strict TypeScript, 12 Vitest files/72 tests, Vite production build, and repository policy. The shared processing-status fixture parses under strict Rust and Zod contracts. Production audit found no known vulnerability, and the 159-package production license inventory parsed.
  - Rust 1.88.0: `pnpm verify:rust` passed locked MSVC rustfmt, Clippy with warnings denied, 54 ordinary tests, and 3 capability tests; two device-dependent probes remain explicitly ignored in the ordinary suite.
  - Generated in-memory tests cover 8/16/24/32-bit PCM, reduced left-aligned valid bits, 32/64-bit float, NaN, mono/stereo/four-channel downmix, malformed alignment and packet lengths, 8/16/22.05/44.1/48/96 kHz conversion, exact finite/clamped output chunks, attenuation above the 8 kHz target Nyquist limit, 10 Hz levels, clipping, format changes, source mismatch, bounded queue pressure, and per-source isolation. No audio fixture or recording is checked in.
  - The exact opt-in Windows processing probe passed with concurrent 44.1 kHz stereo-float microphone and 48 kHz stereo-float render loopback. Both produced and consumed exact 160-sample normalized chunks, emitted level windows, and reported zero capture or processing queue drops, processing errors, non-finite substitutions, and timestamp regressions. Only aggregate metadata/counters were printed; samples were discarded.
  - `pnpm audit:rust` passed Cargo licenses, bans, and sources. RustSec returned success with the same 17 documented allowed maintenance/non-Windows GTK warnings. `rubato` 4.0.0 and its seven locked Rust-only packages introduced no advisory failure or DLL.
  - `pnpm tauri build --ci --no-bundle --target x86_64-pc-windows-msvc -- --locked` produced the final locked x64 release executable. Final formatting, policy, local-link, secret-pattern, artifact, and diff checks passed without retained audio, models, databases, logs, or build artifacts entering the worktree.
- Graphify evidence: the existing ignored graph predates P3-001/P3-002 and contains no processing/resampling vocabulary. A code-graph traversal anchored the Manifest audio flow and ADR 0002; query expansion used `audio`, `format`, `capture`, `windows`, and `source`. The missing local Graphify Python environment prevented a refresh/save, so checked-in architecture, tests, and this task ledger remain authoritative.
- Known limitations: live evidence remains one short stereo-float endpoint pair; deterministic conversion coverage is not a driver/device matrix. Equal-weight downmix ignores channel-mask speaker weights. Reported sinc startup delay is not timestamp-compensated, and partial native/normalized buffers plus filter tail are discarded on format change or stop. The normalized sink intentionally discards samples. Device recovery, two-hour clock drift, VAD, retained audio, Whisper/model management, inference/backpressure, transcript events/UI, and session persistence remain open.

### P3-003 - Source-local VAD and bounded utterance segmentation

- Completed: 2026-08-09
- Deliverables: exact 16 ms Earshot framing at threshold 0.5; independent detector and segmenter state for `microphone` and `system_output`; 300 ms pre-roll; exact 500 ms trailing-silence trimming; 160 ms minimum classified speech; exact 30 s hard split with bounded overlap; format-change, normalized-timeline-discontinuity, finish, and subframe-tail handling; a 485,055-sample per-source memory ceiling; aggregate strict Rust/Zod status diagnostics; an explicit ignored Earshot/Silero v6 comparison; dependency/model/privacy/development/prototype documentation; no new command, capability, UI, event, persistence, retained audio, transcription, catalog/download, or external network path.
- Verification:
  - The acceptance thresholds and one generated in-memory corpus were recorded before measurement. On the first exact run across 625 identical 512-sample/32 ms windows, Earshot 1.2.1 produced 0.40% speech misses, 1.07% non-speech false positives, and 0.9901 F1; Silero VAD v6 produced 52.80%, 0.00%, and 0.6413 respectively. Debug elapsed times were 495 ms and 132 ms and are not throughput claims.
  - Scripted-predictor tests prove exact pre-roll/tail timing, short-speech rejection, exact hard split and bounded overlap, source isolation, discontinuity/format resets, end-of-stream finalization, and preservation of a bounded subframe tail independently of statistical detector behavior.
  - The exact live Windows probe passed on concurrent 44.1 kHz stereo-float microphone and 48 kHz stereo-float render loopback: 355 and 358 VAD frames were classified, all frame-accounting invariants passed, buffers returned to zero, and capture/processing queues and errors remained zero. One 4.64 s system-output utterance finalized at end-of-stream; its 74,240 samples were counted and discarded.
  - Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed formatting, ESLint, strict TypeScript, 12 Vitest files/72 tests, production build, and policy. Rust 1.88 `pnpm verify:rust` passed locked MSVC rustfmt, Clippy with warnings denied, 60 ordinary tests, and 3 capability tests; the bake-off and two hardware probes remain explicitly ignored in the ordinary suite.
  - Frontend production audit found no known vulnerability and its 159-package license inventory parsed. Cargo licenses, bans, and sources passed; RustSec returned success with the same 17 allowed maintenance/non-Windows GTK warnings. The comparison-only `silero`/ONNX Runtime graph is development-only; the locked x64 release build compiled Earshot but not Silero and produced the executable.
- Graphify evidence: the existing ignored graph predates Phase 3 and contains no VAD/speech/silence/segmenter/normalized vocabulary. A fallback traversal connected the Manifest audio flow with ADRs 0002 and 0003; query expansion used `audio`, `capture`, `format`, `source`, `transcription`, and `windows`. The missing local Graphify Python environment/CLI prevented refresh or saved query output, so checked-in architecture, implementation tests, and this ledger remain authoritative.
- Known limitations: the reproducible corpus uses harmonic speech proxies and synthetic non-speech, favors Earshot, and cannot establish accuracy across real speakers, languages, accents, rooms, microphones, echo, overlap, music, keyboards, or device recovery. The live probe is one short endpoint pair and not a labeled accuracy test. VAD frames are quantized to 16 ms. R-003's broader consented/licensed corpus, R-001's device/recovery matrix, R-002's two-hour drift gate, Whisper/model management, inference scheduling/backpressure, retained audio, transcript events/UI, and persistence remain open.

### P3-004 - Bounded local Whisper runtime and Tiny/Base throughput decision

- Completed: 2026-08-09
- Deliverables: strict finalized-utterance request validation; stable source-labelled result/error contracts; bounded 256-segment/1 MiB UTF-8 result copying; one reusable model owner; CPU-only Tiny/Base model kind; project-owned C ABI adapter API v1 over official whisper.cpp v1.9.2; restricted Windows DLL search; one process-lifetime native module with explicit model/result destruction; malformed model-load recovery; local FFmpeg/Earshot throughput runner; SHA-256-pinned model inputs; MP3/model/native-artifact repository policy; implementation, architecture, privacy, dependency, development, and probe documentation; no product wiring.
- Frozen gate and result: the user-supplied 5:06 MP3 decoded to finite clamped 16 kHz mono and the production Earshot segmenter finalized the same seven utterances/86.772 seconds for both models. On an AMD Ryzen 7 3700X with eight inference threads, Tiny loaded in 109 ms and ran in 7,945 ms (RTF 0.0916, p95 1.831 s); Base loaded in 187 ms and ran in 16,696 ms (RTF 0.1924, p95 3.637 s). Both pass the hard RTF <1.0 gate; Tiny alone met the two-second p95 target. Six of seven results contained nonempty text; content and detected language were never printed or retained.
- Failure/ownership evidence: an invalid local model failed with the fixed `transcription_model_load_failed` boundary before both valid model runs succeeded. Unit tests prove invalid/non-finite/oversized audio and timeline mismatch rejection, fixed path-free error strings, and that a microphone inference failure does not poison a following system-output result. Native model and result handles free cleanly. Explicit `FreeLibrary` testing exposed a GGML backend unload fault, so the bounded singleton retains one adapter module for process lifetime and reuses it across Tiny/Base.
- Verification:
  - Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed Prettier, ESLint, strict TypeScript, 12 Vitest files/72 tests, Vite build, and repository policy. Production audit found no known vulnerability; the 159-package production license inventory parsed.
  - Rust 1.88 `cargo fmt --check`, locked MSVC Clippy with warnings denied, and locked all-target/all-feature tests passed: 63 ordinary unit tests plus 3 capability tests; four explicit dependency/hardware/model probes remain ignored in the ordinary suite.
  - The exact `scripts/run-whisper-throughput-prototype.ps1` run verified source commit and model SHA-256 pins, recovered from a malformed model, built the KokoroKoe adapter under MSVC `/W4 /WX`, and passed both CPU model gates. Upstream whisper.cpp emitted only its CMake deprecation warning.
  - Cargo licenses, bans, and sources passed. RustSec returned success with the same 17 allowed maintenance/non-Windows GTK warnings. No Rust package or lockfile entry was added; only existing `windows-sys` feature projections expanded.
  - `pnpm tauri build --ci --no-bundle --target x86_64-pc-windows-msvc -- --locked` produced the final x64 release executable. The prototype DLL/model set is deliberately not bundled yet.
  - Graphify fallback traversal used existing exact vocabulary `transcription`, `whisper`, `audio`, `engine`, and `bounded` and connected the Manifest flow to ADR 0003. The graph predates Phase 3 and the missing Graphify Python environment/CLI prevented refresh or saved query output; checked-in implementation/tests/docs remain authoritative.
- Known limitations: the local MP3 is user data and not a redistributable or labelled accuracy fixture. One high-end desktop CPU is not the minimum-hardware matrix. Base misses the two-second p95 target, and seven utterances are a small sample. The native/model set is external probe material rather than packaged application content. Model catalog/download/install/delete, memory/disk diagnostics, live capture integration, partials, scheduler/backpressure, transcript events/UI, persistence, and Vulkan recovery are all out of scope. Model kind is trusted from the future curated catalog and is not inferred from arbitrary bytes.

### P3-005 - Vulkan failure isolation and CPU recovery

- Completed: 2026-08-09
- Deliverables: explicit CPU/Vulkan adapter API v2; Vulkan-device attestation that rejects silent CPU substitution; fixed `transcription_backend_unavailable`; bounded CPU fallback accounting for startup and inference failures; one generated local 16 kHz speech fixture; fresh-process success/missing-driver/native-abort probes with 60-second child bounds; a pinned copy-only Vulkan SDK runner; ADR 0007 supervised-worker decision; architecture, privacy, dependency, development, and handoff documentation; no product wiring.
- Frozen gate and result: one System Speech utterance decoded to 73,532 finite mono samples (4.596 seconds) and reused under the same `system_output` source and timeline. The NVIDIA RTX 3070 Ti completed an explicitly attested Vulkan inference. `VK_DRIVER_FILES` constrained to a nonexistent manifest caused the fixed backend-unavailable startup result. A prototype-only native abort inside Vulkan inference terminated below the Rust child boundary while the parent remained alive. Startup and inference failure scenarios each produced exactly one CPU result, with zero duplicates and zero losses. The exact gate completed in 24.93 seconds; this aggregate duration is not a latency claim.
- Isolation decision: a native abort is not recoverable in the Tauri process, so Vulkan must run in a supervised local worker. The healthy worker alone owns the accelerated model; after unavailable/terminated worker state releases it, the parent inference owner loads CPU and retries the same finalized utterance once. The product worker protocol, executable, lifecycle integration, and packaging remain future implementation.
- Verification:
  - `scripts/run-whisper-vulkan-recovery-prototype.ps1` verified official `whisper.cpp` commit, Tiny-model SHA-256, and LunarG SDK 1.4.350.0 installer SHA-256; used SDK `copy_only=1`; generated the local fixture; built CPU/Vulkan/fault-injection probe artifacts externally; and passed attestation, missing-driver, native-abort, exact CPU recovery, duplicate, loss, and timeout checks.
  - The KokoroKoe adapter built with MSVC `/W4 /WX` in CPU and Vulkan configurations. Pinned upstream Vulkan/Whisper sources emit their own warnings; none are suppressed or attributed to project-source cleanliness.
  - The P3-004 CPU throughput regression passed under API v2: Tiny RTF 0.0900/p95 1.789 s and Base RTF 0.1895/p95 3.606 s across the same seven utterances.
  - Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed Prettier, ESLint, strict TypeScript, 12 Vitest files/72 tests, Vite build, and repository policy. The production audit found no known vulnerability and the 159-package production license inventory parsed.
  - Rust 1.88 `pnpm verify:rust` passed locked rustfmt, Clippy with warnings denied, 66 ordinary tests, and 3 capability tests; six dependency/hardware/model/worker gates remain explicitly ignored in the ordinary suite.
  - Cargo licenses, bans, and sources passed. RustSec returned success with the same 17 allowed maintenance/non-Windows GTK warnings. P3-005 adds no Cargo/frontend dependency and changes no lockfile.
  - `pnpm tauri build --ci --no-bundle --target x86_64-pc-windows-msvc -- --locked` produced the final x64 release executable without bundling the prototype SDK, model, native DLLs, generated audio, or worker.
- Graphify evidence: the ignored graph predates Phase 3. Exact-vocabulary expansion found only `recovery`, `bounded`, `model`, and `worker`, and traversal reached foundation error/settings nodes rather than transcription. The missing Graphify Python marker/CLI required the documented direct JSON fallback; it confirmed the graph is a dead end for this task, so checked-in architecture, ADR 0007, implementation tests, and this ledger are authoritative.
- Known limitations: the real pass covers one NVIDIA device/driver, Tiny, one generated English utterance, and development binaries. AMD, Intel, multi-GPU, old/broken drivers, device loss, suspend/resume, protocol corruption, timeout recovery, CPU-only/non-Vulkan hosts, and packaged workers remain untested. The deliberate abort represents crash-class isolation, not every driver failure. The future worker must implement bounded IPC and sanitized lifecycle diagnostics without audio persistence or double emission.

### P3-006 - Prioritized transcription scheduler and backpressure soak

- Completed: 2026-08-09
- Deliverables: pure Rust owned-job scheduler; chronological pending-final priority; one replaceable partial per source with round-robin selection; 20-second disable/10-second restore hysteresis; 600-second, 9,600,000-sample, and 4,096-job final ceilings; fixed source/timeline `transcription_final_backlog_exceeded` gaps; duplicate-pending-ID rejection; aggregate bounded diagnostics; deterministic slowdown and two-hour soak; architecture, privacy, dependency, development, and prototype documentation; no product wiring.
- Frozen gate and result: a model-free inference owner required five simulated seconds for each one-second final while one alternating `microphone`/`system_output` final arrived every simulated second for 7,200 seconds. The scheduler admitted 2,040 finals, started 1,440 (720 per source), and returned 5,160 explicit gaps (2,580 per source). The second simulated hour stayed exactly at 600 pending jobs and 9,600,000 logical samples; partial storage never exceeded two jobs and was cleared/suppressed under lag. Started plus pending accepted finals had unique IDs and nondecreasing start timestamps.
- Verification:
  - `cargo test --manifest-path src-tauri/Cargo.toml --locked --target x86_64-pc-windows-msvc transcription::scheduler -- --nocapture` passed eight focused tests in 5.32 seconds and printed the frozen soak metrics.
  - Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 74 ordinary tests, and 3 capability tests; the same six explicit dependency/hardware/model/worker gates remain ignored.
  - Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed Prettier, ESLint, strict TypeScript, 12 Vitest files/72 tests, Vite build, and repository policy.
  - The production frontend audit found no known vulnerability; the 159-package license inventory parsed. Cargo licenses, bans, and sources passed; RustSec returned success with the same 17 allowed warnings. No dependency or lockfile changed.
  - `pnpm tauri build --ci --no-bundle --target x86_64-pc-windows-msvc -- --locked` produced the final x64 release executable.
- Graphify evidence: the stale graph expanded the task to exact vocabulary `transcription`, `backpressure`, `source`, and `bounded`, but traversal reached only ADR 0003 and unrelated package nodes. The Graphify interpreter marker remained absent, so direct JSON traversal was a documented dead end and current Rust/docs were inspected directly.
- Known limitations: logical sample accounting is deterministic but not an allocator/RSS benchmark because the soak shares an immutable generated buffer. The scheduler is not yet wired to capture, VAD, Whisper, the Vulkan worker, commands/events, persistence, or UI. Pending jobs are chronological, but live integration still needs an arrival-watermark policy if one source can deliver older finalized audio after newer inference has begun. Gap persistence/presentation, real inference jitter, thread contention, pause/stop, and worker failures remain future integration work.

### P3-007 - QPC alignment and source-local Windows recovery

- Completed: 2026-08-09
- Deliverables: deterministic two-hour dual-cadence QPC simulation; explicit source-local discontinuity preservation; durable aggregate capture-failure/recovery diagnostics in the strict Rust/Zod fixture-backed contract; one-shot Rust-test-only capture-loop fault; bounded live dual-endpoint recovery probe; architecture, privacy, dependency, development, and prototype documentation; no production injection surface or product wiring.
- Frozen gate and result: one large epoch at 10,000,003 Hz mapped 720,001 microphone timestamps (44.1 kHz, 441-frame cadence) and 675,001 system timestamps (48 kHz, 512-frame cadence) over 7,200 seconds with 0 ms maximum calculated error and zero regressions. A 250 ms microphone discontinuity at one hour remained a 260 ms transition including packet cadence. Two live gate runs injected one microphone failure after both endpoints became active; each restarted on attempt 2, closed one 264-265 ms gap, retained `audio_injected_capture_failure` after the transient error cleared, advanced system output by 51-53 packets, and captured 24-26 more microphone packets after recovery.
- Verification:
  - `cargo test --manifest-path src-tauri/Cargo.toml --locked --target x86_64-pc-windows-msvc audio::timeline -- --nocapture` passed three focused clock tests and printed the accepted aggregate metrics.
  - `cargo test --manifest-path src-tauri/Cargo.toml --locked --target x86_64-pc-windows-msvc audio::windows::tests::hardware_probe_recovers_one_source_without_stopping_the_other -- --ignored --exact --nocapture` passed the live source-isolation/recovery gate.
  - Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 75 ordinary tests, and 3 capability tests; seven explicit dependency/hardware/model/worker gates remain ignored in the ordinary suite.
  - Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed Prettier, ESLint, strict TypeScript, 12 Vitest files/72 tests, Vite build, and repository policy.
  - Frontend audit found no known vulnerability; the 159-package production license inventory parsed. Cargo licenses, bans, and sources passed; RustSec returned success with the same 17 allowed warnings. No dependency or lockfile changed.
  - `pnpm tauri build --ci --no-bundle --target x86_64-pc-windows-msvc -- --locked` produced the final x64 release executable.
- Graphify evidence: the stale graph expanded the exact task vocabulary to `audio`, `capture`, `windows`, `source`, `recovery`, `default`, and `bounded`, but traversal reached only the Manifest audio node and ADR 0002. The Graphify interpreter marker/CLI remained absent, so the graph was a documented dead end and current capture/timeline/contracts/docs were inspected directly.
- Known limitations: the two-hour result calculates conversion/alignment error and does not measure real endpoint oscillator drift or physical acoustic latency. Live evidence covers one default microphone/render pair and one injected microphone-loop failure. Real unplug/hotplug, fixed removal, default changes, Windows Audio restart, sleep/resume, Bluetooth, docks, USB/virtual endpoints, Remote Desktop, other drivers/formats, and repeated/simultaneous failures remain open. Recovery history is aggregate rather than a per-attempt event log, and product capture/transcription/UI/persistence remain unwired.

### P3-008 - Supervised Vulkan worker protocol and lifecycle

- Completed: 2026-08-09
- Deliverables: strict protocol v1 with 4-byte little-endian length-prefixed JSON and a 16 MiB frame ceiling; validated source, timeline, request, segment, language, text, path, and 480,000-sample utterance bounds; one mutable in-flight request; 30-second startup, 5-second write, and 60-second inference/read ceilings with 10 ms cancellation polling; private same-executable worker mode over anonymous pipes; Vulkan backend attestation; kill-on-close Windows Job Object supervision; worker-process and descendant termination; fixed path-free diagnostics; lazy one-shot CPU recovery after worker termination; debug-only corruption/timeout/termination probes; documentation and an exact external runner; no product command, capability, event, UI, persistence, retained audio, model distribution, or external service.
- Frozen gate and result: one healthy worker returned one attested Vulkan result. Seven non-cancelled accelerated failures covered a missing driver, malformed hello, startup hang, malformed response, blocked write, hung inference, and forced nonzero inference termination; every attempt returned exactly one CPU result after worker shutdown. Explicit cancellation returned no CPU retry, a confirmed-started descendant did not survive Job Object teardown, and the totals were one accelerated result, seven failed accelerated attempts, seven CPU results, one cancelled request, zero duplicates, and zero losses. The exact gate completed in 30.63 seconds; this repeated model-startup aggregate is not a latency claim.
- Verification:
  - `scripts/run-whisper-worker-protocol-prototype.ps1` revalidated the pinned Tiny SHA-256, generated 16 kHz float fixture, external adapter/DLL inputs, exact Vulkan attestation, corruption rejection, write/inference timeouts, process termination, cancellation, descendant cleanup, lazy CPU load ordering, and exact result accounting.
  - Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 81 ordinary tests, and 3 capability tests; eight dependency/hardware/model/worker gates remain explicitly ignored in the ordinary suite.
  - Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed Prettier, ESLint, strict TypeScript, 12 Vitest files/72 tests, Vite build, and repository policy.
  - The production frontend audit found no known vulnerability; the 159-package license inventory parsed. Cargo licenses, bans, and sources passed; RustSec returned success with the same 17 allowed warnings. P3-008 adds only Windows API feature projections and changes no lockfile.
  - `pnpm tauri build --ci --no-bundle --target x86_64-pc-windows-msvc -- --locked` produced the x64 release executable. A closed-input worker-mode launch exited immediately with fixed code 20, and the release binary contained none of the debug-only fault-mode or environment strings.
- Known limitations: the exact native pass covers one NVIDIA GPU/driver, Tiny, one generated English utterance, and development-side external native/model assets. The worker is not yet packaged with those assets or wired to live capture, VAD, the scheduler, Tauri commands/events, UI, Markdown/SQLite, or pause/stop controls. CPU fallback is cancellation-aware before it begins, but an in-progress CPU inference is not preemptible. AMD, Intel, CPU-only/non-Vulkan hosts, multi-GPU, device loss, suspend/resume, real broken drivers, worker upgrades, and installer/signed-child behavior remain untested.

### P3-009 - Live transcription ordering and lifecycle integration

- Completed: 2026-08-09
- Deliverables: conservative ordering frontier from active VAD speech, unclassified pending frames, and retained pre-roll; frontier propagation through `ProcessingOutcome`; bounded finalized-VAD handoff; strict cross-source eligibility below both open frontiers; sealed-source infinity; existing start/end/arrival scheduler tie order; result/source/timeline/language/text/segment revalidation; fixed backlog/inference/invalid-result/cancellation gaps; exact result accounting; pause/resume/stop/cancel state machine; cancellation-aware engine trait; supervised-worker/CPU-owner integration; aggregate diagnostics; documentation; no command, event, capability, UI, persistence, retained audio, external model/audio, or network path.
- Frozen gate and result: one delayed source blocked a newer 1,000 ms final until an older 200 ms final arrived, then both ran in order. The two-hour 5x-pressure gate received 7,200 alternating one-second finals, delivered 2,039 chronological results split 1,020 microphone/1,019 system output, and returned 5,161 explicit backlog gaps. It peaked at exactly 600 pending jobs, 600,000 ms of final speech, and 9,600,000 logical samples. Every received ID appeared exactly once as a result or gap. Pause terminally flushed and drained before becoming paused, resume reopened empty frontiers on the same timeline, stop drained before completion, and stop from an already drained pause completed immediately.
- Failure and cancellation evidence: a microphone inference failure became one fixed gap and the following system-output final succeeded. One accelerated-worker termination shut the worker down, loaded CPU once, and returned two CPU results for two finals. A worker cancellation shut down once, loaded CPU zero times, and accounted for both the cancelled in-flight final and pending final as fixed gaps.
- Verification:
  - `cargo test --manifest-path src-tauri/Cargo.toml --locked --target x86_64-pc-windows-msvc transcription::integration -- --nocapture` passed eight focused integration tests and printed the frozen pressure metrics.
  - Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 90 ordinary tests, and 3 capability tests; eight dependency/hardware/model/worker gates remain explicitly ignored in the ordinary suite.
  - Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed Prettier, ESLint, strict TypeScript, 12 Vitest files/72 tests, Vite build, and repository policy.
  - Frontend audit found no known vulnerability and the 159-package production license inventory parsed. Cargo licenses, bans, and sources passed; RustSec returned success with the same 17 allowed warnings. No dependency or lockfile changed.
  - `pnpm tauri build --ci --no-bundle --target x86_64-pc-windows-msvc -- --locked` produced the final x64 release executable.
- Known limitations: generated constant samples, deterministic cadence, and fake bounded text prove orchestration rather than recognition accuracy or real latency. The gate does not measure allocator/RSS, thread/channel contention, real capture recovery during inference, real model jitter, or a physical device/model matrix. Product session ownership, capture startup, partial-result production, commands/events/UI, Markdown/SQLite, model installation, worker packaging/restart policy, and preemptible CPU inference remain future work.

### P3-010 - Curated Whisper model management and installation

- Completed: 2026-08-09
- Owner: primary Codex agent
- Scope: a Rust-only prototype for the curated multilingual Whisper Tiny and Base catalog, pinned source/hash/license provenance, Rust-owned compatibility diagnostics and paths, resumable bounded download staging, exact length/SHA-256 verification, same-volume atomic installation, installed-model discovery, ephemeral default selection, explicit deletion, and interrupted/corrupt-download recovery. No Tauri command/event, React UI, generic frontend filesystem/HTTP/process permission, live-session wiring, transcript persistence, retained audio, OpenRouter path, native-runtime packaging, or installer/release claim is in scope.
- Dependencies: accepted architecture and ADRs 0002/0003/0007; P3-004 verified Whisper adapter/model evidence; P3-008 worker path contract; P3-009 lifecycle integration; Rust 1.88; app-local-data model root; `reqwest` blocking Rust HTTP with Rustls; `sha2`; `fs2` free-space checks.
- Acceptance criteria:
  - The only catalog entries are multilingual Tiny and Base, pinned to immutable Hugging Face revision `5359861c739e955e79d9a303bcbc70fb988958b1`, exact byte lengths and SHA-256 digests, and MIT provenance; model IDs, filenames, and derived paths are constants rather than caller-controlled filesystem input.
  - A partial staging file resumes only after strict metadata/length validation. HTTP `206` must start at the requested byte; `200` restarts safely; cancellation leaves resumable state; invalid range/protocol responses fail closed without installing bytes.
  - Installation requires exact length and full-file SHA-256, flushes the staging file, and uses a same-directory atomic rename. A valid existing install is idempotent; corrupt staged or installed content cannot be selected or reported as installed and has a bounded recovery path.
  - Rust reports exact remaining download/disk requirements and approximate memory compatibility. Insufficient disk blocks download before network transfer; insufficient memory is a visible compatibility warning rather than an unsupported hard guarantee.
  - Selection rejects uninstalled/corrupt models and remains prototype-memory-only. Deletion addresses only exact catalog-derived files, rejects the selected model, and clears resumable/corrupt artifacts without following caller paths.
  - Deterministic tests cover catalog immutability, clean install, resume, restart-on-ignored-range, cancellation/resume, corruption, disk/memory diagnostics, selection/deletion, and path containment. An explicit ignored local gate verifies both catalog hashes against the existing external P3-004 model files without checking model bytes into the repository.
- Deliverables: exact two-entry Tiny/Base catalog at immutable Hugging Face revision `5359861c739e955e79d9a303bcbc70fb988958b1`; exact byte/SHA-256/MIT/language/backend metadata; app-local fixed-name model root; Rustls-backed blocking download source; strict HTTP full/partial response handling; bounded 64 KiB streaming; synced strict resume sidecar; disk and physical-memory compatibility diagnostics; exact whole-file hash verification; same-directory atomic rename; idempotent discovery; memory-only verified selection; selected-model deletion denial; bounded corrupt-stage/install cleanup; explicit external hash runner; architecture, dependency, privacy, development, and prototype documentation; no product command/event/UI/persistence/native packaging path.
- Recovery evidence: clean and repeated install, valid partial append, safe restart after ignored range, cancellation after one bounded read and later resume, complete verified staging without another request, malformed resume metadata, corrupt staged bytes, corrupt installed bytes, insufficient disk before source open, verified selection/deletion, unknown-ID containment, and malformed content ranges all pass deterministic tests using only generated tiny bytes and temporary directories.
- Exact model evidence: `scripts/run-whisper-model-installation-prototype.ps1` read the existing external P3-004 files and accepted Tiny at 77,691,713 bytes/SHA-256 `be07e048...1b21` and Base at 147,951,465 bytes/SHA-256 `60ed5bc3...2efe`; no model was copied, downloaded, logged, persisted in the repository, or packaged.
- Verification:
  - Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 102 ordinary tests, and 3 capability tests; nine explicit dependency/hardware/model/worker gates remain ignored in the ordinary suite.
  - Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed Prettier, ESLint, strict TypeScript, 12 Vitest files/72 tests, Vite build, and repository policy. The production npm audit was clean and the 159-package frontend license inventory parsed.
  - Cargo licenses, bans, and sources passed after explicitly accepting the permissive ISC/MIT-0 terms required by Rustls/AWS-LC. RustSec returned success with the same 17 allowed maintenance/non-Windows GTK warnings. `reqwest` 0.13.4 and `sha2` 0.11.0 add 37 locked packages on the resolved graph.
  - `pnpm tauri build --ci --no-bundle --target x86_64-pc-windows-msvc -- --locked` produced the final x64 release executable without models or native Whisper runtime assets.
- Known limitations: deterministic fake-source tests prove state handling but not a real slow/interrupted public download, proxy/authenticated network, bandwidth throttling, server revision changes, disk-full race, power loss, or concurrent jobs. Cancellation is observed between bounded blocking reads; product integration must run the blocking client off the Tauri async/UI thread. Approximate memory values are guidance, not measured RSS or minimum-hardware verdicts. Selection and download state are not persisted; commands/events/UI, worker model-path wiring, native DLL/model packaging, redistribution notices, and installer behavior remain future tasks.

### P3-011 - Product model-management integration

- Status: completed 2026-08-10
- Owner: primary Codex agent
- Scope: expose the accepted P3-010 Tiny/Base manager to the trusted `main` window through the six architecture-defined commands (`list_transcription_models`, `download_transcription_model`, `cancel_model_download`, `resume_model_download`, `delete_transcription_model`, and `set_default_transcription_model`), a versioned `model-download-progress` event, bounded SQLite operational state, dedicated blocking work, strict Rust/Zod adapters, and an accessible Settings model-management surface. Do not wire session capture/transcription, inference model paths, partial/final transcript events, Markdown project/session persistence, retained audio, OpenRouter, native Whisper/DLL/model packaging, or installer/release claims.
- Dependencies: P3-010; persisted versioned settings from P2-003; sanitized command errors/logging; exact `main` authorization and capability tests; TanStack Query; Tauri 2.11.5 emitter/listener APIs; Rust 1.88 and RFC 3339 UTC formatting.
- Acceptance criteria:
  - Rust and Zod parse one shared golden model-installation/job/event fixture exactly, reject unknown fields, unsafe integers, malformed UUID/RFC 3339 values, unbounded strings/collections, invalid enum casing, nullable optionals, inconsistent byte counts, and event/request identity mismatches.
  - All six commands authorize the exact `main` label before cloning state, opening SQLite, starting network/filesystem work, cancelling a job, deleting bytes, or changing settings. The main capability grants only the existing settings/audio commands plus these six exact commands and no generic filesystem, HTTP, shell, process, dialog, or remote capability.
  - At most one model download runs at once. It starts through a dedicated blocking worker, uses the P3-010 cancellation token and resumable staging, emits bounded monotonic progress only to `main`, and terminally accounts for completed, cancelled/paused, or failed work. Event emission failure cannot corrupt or abort the Rust-owned installation.
  - SQLite retains at most 64 strict non-secret job snapshots plus verified installation timestamps. Startup converts persisted `queued`, `downloading`, or `verifying` work to `paused`, reconciles staged bytes and verified final files, and never treats SQLite alone as proof that a model is installed.
  - Default selection requires a verified installed catalog model and an exact expected settings revision. The generic `update_settings` command cannot bypass that validation. Deleting the selected default or a model with an active job is denied; deletion remains catalog/path-derived and clears its operational cache state.
  - The Settings UI displays both curated models, sizes, disk use, languages, performance class, approximate memory, compatibility, installation/default state, bounded progress, sanitized failures, and explicit download/resume/cancel/select/delete actions. It exposes no source URL, hash, local path, raw HTTP response, or resume metadata.
  - Deterministic Rust service tests use a fake backend for progress, cancellation/resume, persistence/restart, selection conflict, and deletion. Vitest covers strict adapters, event reconciliation, and user actions. Full locked frontend/Rust gates, audits, policy, exact capability checks, x64 release build, and a Windows UI smoke pass.
- Verification evidence:
  - The shared `model-management-v1.json` fixture round-trips through strict Rust and Zod contracts. Negative tests reject unknown/nullable fields, unsafe counts, inconsistent byte totals, malformed casing, and mismatched request/event identities.
  - Six exact Tauri commands and their generated allow/deny permissions are restricted to the exact `main` label. The capability gate confirms no generic filesystem, HTTP, shell, process, dialog, remote, or `core:default` grant.
  - Fake-backend Rust tests cover single-flight queuing, throttled progress through verification, cancellation with resumable bytes, persisted restart recovery to paused, verified install reconciliation, revision-conflict selection, deletion safety, and removal of job/install cache state. SQLite retention is capped at 64 strict snapshots.
  - Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed Prettier, ESLint, strict TypeScript, 14 Vitest files/77 tests, Vite production build, and repository policy. UI tests cover bounded progress/actions and prove the Settings DOM does not render source URLs or hashes.
  - Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 109 ordinary tests plus 3 capability tests; nine explicit hardware/model/worker gates remain ignored. The production npm audit is clean, 159 frontend licenses parse, Cargo licenses/bans/sources pass, and RustSec returns success with 17 existing allowed maintenance/non-Windows warnings.
  - `pnpm tauri build --ci --no-bundle --target x86_64-pc-windows-msvc -- --locked` produced the x64 release executable. A bounded launch smoke confirmed the native process remained healthy for five seconds before intentional termination.
- Known limitations: no real model download was started during closeout, so public-network/proxy behavior and interactive cancellation timing remain covered by the P3-010 fake-source and P3-011 fake-backend tests rather than a product UI run. The native computer-control and in-app-browser runtimes both failed during required initialization in this Codex session, so a visual Windows UI smoke could not be automated; component interaction tests and the native release launch smoke are the recorded substitute evidence. P3-011 does not package the native Whisper runtime or models and does not connect model selection to live capture/transcription sessions.

### P3-012 - Product audio-device listing and independent tests

- Status: completed 2026-08-11
- Owner: primary Codex agent
- Scope: replace the four exposed dual-capture prototype commands with the architecture-defined `list_audio_devices`, `start_audio_device_test`, and `stop_audio_device_test` product boundary; add independent source-local microphone/system-output test ownership; emit only bounded aggregate `audio-level-updated` and `audio-device-status-changed` events to the exact `main` window; and add transient default-role/fixed-endpoint selection plus independent controls in Settings. No meeting session, transcript/audio persistence, sample event, OpenRouter path, native Whisper packaging, or generic frontend filesystem/HTTP/shell/process permission is in scope.
- Dependencies: P3-001 capture/enumeration, P3-002 processing/levels, P3-007 recovery/default-policy diagnostics, ADR 0002, exact-main authorization/capability patterns, strict Rust/Zod/event patterns, and active Windows capture/render endpoints.
- Acceptance criteria:
  - One shared fixture covers device enumeration, device-test input/status, aggregate level events, and health events. Rust and Zod enforce bounded endpoint/name/count/level/timestamp fields, strict enum/unknown-field handling, source/direction consistency, failed-status/error consistency, and request/test identity.
  - Only the three exact product commands are registered and permitted. Exact `main` authorization occurs before state access or Windows audio work, and the capability adds no generic filesystem, HTTP, shell, process, dialog, remote, or `core:default` grant.
  - Tests own separate bounded packet/normalized queues and source-local capture/processing threads. At most one test runs per source, both sources may run together, repeated stop is idempotent for a bounded retained status window, and stopping/failing one source does not stop the other.
  - Default-role selection inherits the existing source-local re-resolution policy; fixed selection addresses only the selected endpoint and never silently switches. No audio sample or utterance crosses the Rust boundary or reaches storage/logging.
  - The Settings UI lists input/output endpoints, offers all three default roles plus fixed endpoints, starts/stops each source independently, reconciles only matching request/source events, shows health and peak dBFS, and explicitly states that choices are transient and do not start/save a meeting.
- Deliverables: exact product command registration and generated ACL files; strict product Rust/Zod contracts and golden fixture; bounded `AudioDeviceTestService`; main-window event monitor with 50 ms polling over the existing 100 ms level windows; source/request-aware Tauri adapters; two-panel Settings controls; component/adapter/contract/capability tests; one explicit Windows hardware gate; and removal of the exposed prototype command permissions.
- Verification evidence:
  - Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed Prettier, ESLint, strict TypeScript, 15 Vitest files/76 tests, Vite production build, and repository policy. Component tests cover fixed/default selection, concurrent source tests, matching event reconciliation, stopping by request ID, and the no-session/no-retention notice.
  - Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 111 ordinary tests plus 3 capability tests; 10 dependency/hardware/model/worker gates remain ignored in the ordinary suite.
  - The explicit product hardware test ran both default-console sources concurrently, observed aggregate levels, stopped the microphone test, proved the system-output level timeline continued advancing, and then stopped output cleanly. The probe retained and printed no samples.
  - The main capability contains only settings, model-management, and the three product audio commands. Static tests prove the old prototype permissions are gone and no generic privileged grant was added.
  - Production npm audit found no vulnerability; 159 frontend licenses parsed; Cargo licenses/bans/sources passed; RustSec returned success with the same 17 allowed maintenance/non-Windows warnings.
  - The locked x64 no-bundle release build completed, and the hidden release executable remained responsive for a bounded five-second launch smoke.
- Graphify evidence: the existing ignored graph predates Phase 3. Exact-vocabulary expansion used `audio`, `device`, `capture`, `source`, `settings`, `command`, `event`, `status`, `test`, and `prototype`, but reached only foundation settings/command nodes. The configured Graphify Python interpreter is unavailable on this platform, so refresh/reflection/save could not run; current architecture, ADR 0002, implementation, and tests were inspected directly.
- Known limitations: live evidence covers one short default-console microphone/render pair on one Windows machine and uses a generated system beep to ensure loopback activity. Fixed-endpoint capture, real default switches, unplug/replug, Windows Audio restart, suspend/resume, Bluetooth/dock/USB/virtual/RDP endpoints, disabled/unplugged enumeration states, and the broader format/device matrix remain open hardware work. Device choices are intentionally transient because the accepted interface has no device-preference mutation command; future session/device-preference ownership must persist them without inventing a settings bypass. Native visual automation was not repeated after its P3-011 environment failure; strict component interaction tests plus the native launch smoke are the UI evidence. P3-012 does not create sessions, retain audio, emit transcript results, load an installed model, or package native Whisper assets.

### P3-013 - Live-transcription readiness integration

- Status: completed 2026-08-11
- Owner: primary Codex agent
- Scope: connect independent Rust-owned microphone/system-output processing outcomes to the existing VAD/scheduler/worker-or-CPU coordinator, resolve the configured default only after exact model verification, and stream source-labelled final results or explicit gaps into a bounded in-memory readiness sink. This is a Rust-only readiness seam: no product session command, transcript event/UI, meeting persistence, retained audio, OpenRouter request, generic frontend permission, or native packaging claim is in scope.
- Dependencies: P3-009 coordinator, P3-011 verified default-model state, P3-012 independent Windows capture/process ownership, ADRs 0002/0003/0007, stable `microphone`/`system_output` identifiers, and an external P3 native adapter for the explicit live gate.
- Acceptance criteria:
  - A Rust-only resolver reads the configured default model, accepts only the curated multilingual Tiny/Base identifiers, re-verifies the exact installed artifact, and returns its path only to the local runtime factory. No path, URL, hash, transcript, or sample crosses the frontend/logging/persistence boundary.
  - Capture callbacks remain non-blocking. Per-source processors forward terminal and non-terminal VAD outcomes through fixed-capacity queues; queue closure/pressure and inference failures produce fixed source/timeline gaps instead of silently losing an accepted final.
  - One coordinator owns the model engine and consumes both sources fairly. Its bounded output sink streams records to a consumer rather than accumulating a session transcript; every admitted final is delivered exactly once as a source-labelled result or explicit gap in nondecreasing timeline order.
  - Stop accepts each processor's terminal VAD flush, seals both source frontiers, drains accepted finals/gaps, and joins capture, processing, coordination, and sink work within fixed bounds. Cancellation accounts pending work as fixed gaps and never invokes CPU recovery for cancelled accelerated work.
  - Deterministic tests prove delayed cross-source ordering, exact-once accounting, source-local failure isolation, handoff/output pressure visibility, cancellation, and bounded shutdown. One ignored Windows gate requires both default-console endpoints plus a verified selected local model and external adapter, uses `public/audio_test/OpenAI.mp3` as the playback fixture, proves both capture streams remain active, and requires at least one non-empty source-labelled local final without retaining or printing transcript text.
- Graphify evidence: exact-vocabulary expansion used `audio`, `capture`, `transcription`, `worker`, `source`, `result`, and `windows`. The stale pre-Phase-3 graph reached only Manifest/ADR/foundation nodes, so current Rust contracts, accepted ADRs, and P3-009 through P3-012 evidence are authoritative for this bounded integration.
- Deliverables: exact verified-default artifact resolution with canonical model-root containment and full rehash; a CPU runtime factory accepting only curated Tiny/Base artifacts; fixed-capacity audio-to-VAD and coordinator-to-sink handoffs; a single model-owning coordinator thread over the P3-009 scheduler; terminal source flush/seal/drain behavior; cancellation and fixed gap accounting; deterministic ordering/isolation/pressure tests; and `scripts/run-live-transcription-readiness.ps1` for the explicit installed-model/hardware gate.
- Verification evidence:
  - Deterministic coordinator tests delayed one source frontier, delivered two finals in chronological order with unique job IDs, converted a microphone inference failure to one fixed gap while system output continued, accounted a cancelled pending final without inference, and held the output sink at capacity one until its consumer drained it without loss or duplication.
  - The model-service resolver read the configured default, rejected an uninstalled default, canonicalized the catalog-derived path inside the model root, and reverified exact size/hash before the local runtime received it. The live factory accepts only the two curated multilingual identifiers; paths remain Rust-only.
  - `scripts/run-live-transcription-readiness.ps1` played `public/audio_test/OpenAI.mp3` while the ignored gate used the product settings database, verified selected Tiny artifact, external P3-004 CPU adapter, and both default-console endpoints. It observed 2,394 microphone packets and 2,398 system-output packets, produced one non-empty source-labelled final, returned zero gaps, and printed/retained no transcript text or samples.
  - Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 117 ordinary tests plus 3 capability tests; 11 explicit dependency/hardware/model/worker gates remain ignored in the ordinary suite. Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed formatting, lint, strict types, 15 Vitest files/76 tests, production build, and repository policy.
  - Production npm audit found no vulnerability; 159 frontend licenses parsed; Cargo licenses/bans/sources passed; RustSec returned success with the same 17 allowed maintenance/non-Windows warnings. The locked x64 no-bundle release build completed, and the hidden release executable remained healthy for a bounded five-second launch smoke.
- Known limitations: the live gate covered one machine, the selected Tiny model, the CPU adapter, default-console endpoints, one fixture playback, and one final from one source; it does not claim independent speech finals from both sources, Base/Vulkan live behavior, fixed devices, unplug/recovery, long-run pressure, broader audio hardware, or packaged native assets. Fixed-capacity handoffs intentionally backpressure source-processing threads while capture callbacks remain non-blocking and existing packet-drop diagnostics remain visible. Partial transcription is still discarded by the accepted P3-009 final-only coordinator, and no product session command, transcript event/UI, meeting persistence, retained audio, or OpenRouter path exists yet.

### P3-014 - Partial-transcription readiness

- Status: completed 2026-08-11
- Owner: primary Codex agent
- Scope: add source-local active-speech snapshots to the existing VAD/processing output, submit them through the fixed-capacity P3-013 handoff to the scheduler's already accepted one-per-source replaceable partial slots, and reconcile provisional inference with the matching final inside the Rust-only coordinator. No Tauri command/event/capability, React UI, session/project domain, Markdown/SQLite transcript persistence, retained audio, OpenRouter, or native packaging work is in scope.
- Dependencies: P3-003 VAD bounds and minimum speech, P3-006 partial slots/hysteresis/final priority, P3-009 final ordering/lifecycle integration, P3-013 live handoff/verified runtime, ADR 0003, and stable `microphone`/`system_output` identifiers.
- Acceptance criteria:
  - Freeze active snapshot admission at at least 160 ms of classified speech and a 500 ms replacement cadence. A snapshot includes the source-local pre-roll/active samples already owned by VAD, carries an exact 16 kHz timeline, never exceeds 480,000 samples/30 s, and is absent before the threshold or when no active utterance exists.
  - The live handoff carries at most one optional snapshot per processing update. The scheduler retains at most one partial job per source; replacement uses one stable job identity for the active utterance, and the matching final reuses that identity so a consumer can replace rather than append provisional output.
  - Eligible finals always run before partials. Partials are suppressed at 20 s of queued final speech and restored only below 10 s through the existing scheduler hysteresis. Invalid, failed, stale, replaced, stopped, or cancelled partial work is diagnostic-only and must not create a durable transcript gap or trigger CPU recovery after cancellation.
  - Pause/stop terminal flushes reconcile any matching active snapshot with its final and discard remaining provisional slots before the boundary settles. Cancellation discards provisional work and explicitly accounts only accepted finals as gaps.
  - Deterministic tests prove cadence/sample bounds, stable identity and final replacement, source-local slot isolation, final precedence, hysteresis, inference failure isolation, terminal/cancellation cleanup, and bounded output pressure. The ignored selected-model Windows fixture gate uses `public/audio_test/OpenAI.mp3`, requires at least one non-empty partial and matching non-empty final, and retains/prints only aggregate counts.
- Graphify evidence: required vocabulary expansion found only `audio`, `source`, and `transcription`; the stale graph has no `partial`, VAD, snapshot, utterance, or scheduler vocabulary and reaches only the Manifest flow, architecture flow, and ADR 0003. The Graphify CLI/interpreter is unavailable, so reflection/save could not run; current Rust modules and accepted task evidence are authoritative.
- Deliverables: source-local active-utterance snapshots after 160 ms of classified speech; a fixed 500 ms replacement cadence; exact 16 kHz bounded sample/timeline contracts; optional snapshot transport through the existing fixed-capacity audio handoff; stable source/start-derived provisional job identity; final-first coordinator pumping; diagnostic-only partial failures; terminal/cancellation cleanup; and an upgraded aggregate-only selected-model fixture runner. No new command, event, capability, frontend surface, persistence, retained audio, OpenRouter path, or packaged native-runtime claim was added. The behavior implements the already accepted ADR 0003/P3-006 scheduler direction, so no new ADR was required.
- Verification evidence:
  - Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 120 ordinary tests, and 3 capability tests; 11 explicit dependency/hardware/model/worker gates remain ignored in the ordinary suite.
  - `scripts/run-live-transcription-readiness.ps1` played `public/audio_test/OpenAI.mp3` while the ignored selected-Tiny/CPU gate observed 2,394 microphone packets and 2,399 system-output packets, produced one non-empty partial and one non-empty final with the same job identity, returned zero gaps, and printed/retained no transcript text or samples.
  - Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed formatting, lint, strict types, 15 Vitest files/76 tests, production build, and repository policy. Production npm audit was clean, 159 frontend licenses parsed, Cargo licenses/bans/sources passed, and RustSec returned success with the existing 17 allowed maintenance/non-Windows warnings.
  - The locked x64 no-bundle release build passed, and the native executable remained alive for the bounded five-second launch smoke.
- Known limitations: this remains an internal Rust readiness seam. It does not yet expose product live-session commands/events or render real-time transcript UI. The explicit fixture gate covers one machine, one selected Tiny CPU runtime, one microphone/render pair, and one short playback; broader devices, minimum hardware, Base, Vulkan, long sessions, and product interaction remain later gates.

### P3-015 - Product transient live transcription

- Status: completed 2026-08-11
- Owner: primary Codex agent
- Scope: expose the accepted P3-014 pipeline as one transient, non-persistent live transcription run in the trusted `main` window. Add `start_live_transcription`, `get_live_transcription_status`, and `stop_live_transcription`; strict ordered `transcription-partial`, `transcription-final`, and `transcription-gap` events; and one bounded React transcript surface that replaces provisional text by stable ID. This task does not create a project/session record, Markdown/SQLite transcript, retained audio, OpenRouter request, additional native window, generic frontend permission, or packaged native runtime.
- Dependencies: P3-011 verified selected-model resolution; P3-012 product device selections and consent language; P3-013 bounded live coordinator; P3-014 partial/final identity; ADRs 0002/0003/0007; shared Rust/Zod/error/event patterns; exact `main` authorization and capability tests.
- Acceptance criteria:
  - One strict shared fixture covers start input, idle/running/stopped status, partial/final/gap envelopes, source/timeline/language/text bounds, stable segment replacement identity, monotonically increasing safe event sequence, fixed gap codes, and unknown/null/malformed rejection in both Rust and Zod.
  - `start_live_transcription` requires a fresh request UUID, explicit capture/transcription-law acknowledgement, valid independent microphone/system-output selections, and the configured verified local model. `get_live_transcription_status` is read-only. `stop_live_transcription` addresses only the active/retained request, is idempotent for the retained terminal status, and drains terminal VAD/final work before reporting stopped.
  - All three commands authorize the exact `main` label before cloning service/model state or starting/stopping audio/model work. The capability adds only their generated permissions and no generic filesystem, HTTP, shell, process, dialog, remote, or `core:default` grant.
  - Rust targets every event only to the invoking `main` webview. A run-local sequence orders delivery; one UUID is allocated for each internal job ID and reused by its final. Final payloads identify whether they replace an observed partial. Partial inference errors/replacements remain non-durable; accepted final failures become bounded source/timeline gap events.
  - The React surface validates every command result/event, renders transcript text only as inert plain text, labels `microphone` as You and `system_output` as System, shows timestamps and provisional state, replaces rather than appends matching finals, ignores mismatched request/non-increasing-sequence events, and retains at most the newest 500 segment/gap records. It provides explicit consent, start/stop, return-to-latest behavior, and a clear no-persistence/no-audio-retention notice.
  - Deterministic service/command/adapter/component tests prove single-flight lifecycle, authorization-before-state, event ordering/identity, malformed-event rejection, source labelling, partial replacement, final/gap rendering, bounded UI memory, and stop cleanup. An explicit Windows selected-model/`OpenAI.mp3` gate proves product event conversion with at least one non-empty partial and matching final while printing/retaining only aggregate counts. Full locked frontend/Rust/capability gates, audits, x64 release build, and launch smoke must pass.
- Graphify evidence: exact-vocabulary expansion used `transcription`, `session`, `command`, `event`, `frontend`, `react`, `audio`, `source`, `start`, `status`, `consent`, and `window`. The stale graph reached the Manifest real-time window and foundation exact-main command authorization but has no partial/final/stop/transcript vocabulary. The Graphify CLI and recorded interpreter are unavailable on this platform, so reflection/save could not run; current contracts, accepted ADRs, implementation, and tests are authoritative.
- Deliverables: three exact main-authorized commands; fixed main-targeted partial/final/gap events with run-local sequencing and stable segment replacement identity; verified-default CPU runtime startup; single-flight start/status/idempotent-stop ownership; strict Rust/Zod shared contracts and Tauri adapters; three exact generated capability grants; consent-gated transient transcript UI with inert source-labelled text, timestamps, gaps, return-to-latest, stale-event rejection, and a 500-record cap; deterministic lifecycle/event/contract/adapter/component/capability tests; and `scripts/run-product-live-transcription.ps1`. No project/session record, Markdown/SQLite transcript, retained audio, OpenRouter request, extra window, generic frontend permission, or packaged model/native asset was added. The accepted architecture now records the deliberate transient-run boundary, so no ADR was required.
- Verification evidence:
  - Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed formatting, ESLint, strict TypeScript, 18 Vitest files/83 tests, production build, and repository policy.
  - Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 126 ordinary tests, and 3 capability tests; 12 explicit dependency/hardware/model/worker gates remain ignored in the ordinary suite.
  - `scripts/run-product-live-transcription.ps1` played `public/audio_test/OpenAI.mp3` while the product service used the verified selected Tiny model, external CPU adapter, and both default endpoints. It emitted 45 partial updates, one non-empty final that replaced a non-empty partial with the same product segment UUID, and zero gaps. The gate also proved single-flight rejection, terminal stop, and idempotent repeated stop while printing only aggregate counts and retaining no audio/transcript artifact.
  - Production npm audit found no known vulnerability; 159 frontend production licenses parsed; Cargo licenses/bans/sources passed; RustSec returned success with the existing 17 allowed maintenance/non-Windows warnings. The locked x64 no-bundle release build passed, and the hidden executable remained responsive for the five-second launch smoke.
- Known limitations: the product gate covers one Windows machine, the selected Tiny CPU model, default endpoints, and one short fixture. It does not cover Base/Vulkan product operation, fixed devices, unplug/default-switch/recovery, protected audio, broader hardware, long sessions, or minimum-spec latency. Stop drained the accepted backlog before returning and the complete gate took about 59 seconds including 24 seconds of capture; the UI exposes the stopping state but no cancellation shortcut. Native Whisper assets and models remain deliberately unbundled, so a development/release app must have `kokorokoe_whisper_adapter.dll` beside its executable and a verified selected model in app-local state or start returns a fixed sanitized unavailable error. The transcript is intentionally transient and disappears on navigation/process exit; Phase 4 must replace this preview boundary with project/session persistence without turning SQLite into transcript source of truth.

### Phase 3 - Audio and transcription completion

- Completed: 2026-08-11
- Audit result: P3-001 through P3-015 collectively satisfy the Phase 3 implementation gate. Evidence covers simultaneous independent WASAPI sources, source-local recovery, a shared QPC timeline, supported native-format normalization, VAD/segmentation, Tiny/Base CPU throughput, explicit Vulkan attestation with supervised-worker CPU fallback, bounded/fair final scheduling and partial shedding, exact-once partial/final/gap reconciliation, verified model selection, product device tests, and a nonblocking transient product UI.
- Gate evidence: deterministic two-hour clock/backpressure simulations; explicit Windows dual-source, recovery, model, worker, and selected-Tiny product gates; strict Rust/Zod contracts; exact-main ACL tests; locked frontend/Rust checks, audits, x64 builds, and launch smokes recorded under the individual Phase 3 tasks.
- Deferred validation: the broader physical device/driver matrix, real endpoint oscillator/acoustic behavior, minimum supported CPU, Base/Vulkan product breadth, long live sessions, and bundled native assets remain Phase 7 quality/package gates. These limitations reduce release confidence but do not require a different Phase 3 architecture seam before portable session ownership begins.
- Boundary carried forward: the P3-015 live transcript remains intentionally transient. Phase 4 must add project/session ownership and crash-resilient Markdown persistence before it can replace that preview; audio retention, OpenRouter, additional windows, and native asset packaging remain outside P4-001.

### P4-001 - Project/session domain and portable folder contract

- Status: completed 2026-08-11
- Owner: primary Codex agent
- Scope: freeze strict version-one `Project` and `Session` records plus their portable relative folder/document layout before any project/session filesystem service or product command is introduced. Include frozen preset and resolved device snapshots, role-specific optional LLM model identifiers, lifecycle/channel/summary/usage state, UUID/revision/timestamp bounds, stable ASCII folder identities, and the architecture-defined Markdown/journal/audio relative names. Do not create a directory, write Markdown, add a project/session SQLite row, expose a Tauri command or capability, wire P3-015 into a persisted session, retain audio, call OpenRouter, add another window, or package native runtime assets.
- Dependencies: completed Phase 3 audit; Manifest project/session and Markdown sections; ADR 0001 Markdown ownership, ADR 0005 workspace/path policy, ADR 0006 untrusted Markdown boundary; architecture domain/layout contracts; existing `DeviceSelection`, `PresetId`, strict Serde/Zod, RFC 3339, and shared-fixture patterns.
- Acceptance criteria:
  - Rust and Zod parse and round-trip one shared fixture containing a project, session, and exact derived layout. They reject unknown fields, explicit null optionals, malformed UUID/RFC 3339/BCP-47/fixed-decimal values, unsafe revisions/token counts, control characters, duplicate bounded collections, invalid enum casing, reversed timestamps, completed sessions without an end time, fixed-device snapshot mismatches, folder/UUID/date mismatches, cross-project sessions, and altered derived paths.
  - Project folders use `<portable-slug>--<project-uuid8>` and session folders use `<yyyy-mm-dd>-<portable-slug>--<session-uuid8>`. Slugs contain only lowercase ASCII letters, digits, and single separators, are bounded to 48 bytes, and fall back to `project` or `session` when a title has no ASCII alphanumeric content. Persisted folder names remain independent of later display-name edits.
  - The fixed relative layout contains `project.md`, `presets/`, `sessions/`, and per-session `session.md`, `transcript.md`, `summary.md`, `insights.md`, `actions.md`, `questions.md`, `recovery.journal`, and `audio/`. Every derived value starts below `projects/`, contains only validated components and `/` separators, and accepts no caller-supplied path.
  - P4-001 performs no sensitive filesystem mutation. ADR 0005 handle identity, reparse/containment revalidation, atomic writes, journals, snapshots, SQLite rebuild, deletion, export, commands, and UI remain separately testable later Phase 4 tasks.
- Graphify evidence: the required exact-vocabulary expansion used `project`, `session`, `folder`, `workspace`, `settings`, `domain`, `contract`, `markdown`, `persistence`, `path`, `sqlite`, and `preset`. The stale graph reached the P2-003 workspace/settings validation and strict domain contract nodes but predates Phase 3 and contains no current project/session implementation. Its recorded `python` interpreter is invalid on this Windows host, so reflection and save-result were unavailable; current architecture, ADRs, source, fixture, and tests are authoritative.
- Deliverables: `ProjectId`/`SessionId`; strict Project/Session, preset/device/channel/usage/lifecycle types; mirrored branded Zod contracts; deterministic bounded folder-name derivation and validation; exact portable relative layout generation; a shared version-one golden fixture; adversarial cross-field/path tests; Phase 3 completion record; architecture/checkpoint updates. No new dependency, command, event, permission, database migration, UI route, filesystem write, Markdown renderer input, retained audio, transcript artifact, OpenRouter path, or native asset was added.
- Verification evidence:
  - Targeted Rust tests round-trip the shared project/session/layout fixture, prove folder derivation including a non-ASCII fallback, reject unknown/malformed/null/unsafe/inconsistent records, constrain every layout value below `projects/`, and reject a session belonging to another project.
  - Targeted Vitest tests parse the same fixture and reject invalid project/session records, fixed-endpoint mismatch, altered layout, and cross-project identity.
  - Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed formatting, ESLint, strict TypeScript, 19 Vitest files/98 tests, production build, and repository policy. Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 131 ordinary tests, and 3 capability tests; 12 explicit dependency/hardware/model/worker gates remain ignored in the ordinary suite.
  - Production npm audit found no known vulnerability; 159 frontend production licenses parsed; Cargo licenses/bans/sources passed; RustSec returned success with the existing 17 allowed maintenance/non-Windows warnings. The locked x64 no-bundle release build completed, and the hidden executable remained alive and responsive for the bounded five-second launch smoke.
- Known limitations: P4-001 freezes records and relative names only. It does not yet pin an open workspace directory handle, create portable Markdown/YAML, implement atomic replacement or a checksummed journal, persist/rebuild a SQLite/FTS projection, create/edit/list/delete/export projects or sessions, recover interrupted sessions, or connect the P3-015 coordinator to durable records. The ASCII slug fallback may produce similar human-readable prefixes, but the UUID suffix preserves uniqueness and identity.

### P4-002 - Pinned project creation and `project.md` snapshot

- Status: completed 2026-08-11
- Owner: primary Codex agent
- Scope: add the first Rust-owned Phase 4 content mutation by pinning a validated Windows workspace, creating only the P4-001-derived project directory, and publishing one strict version-one `project.md` YAML-front-matter snapshot. Exclude reading/updating projects, sessions, transcript journals, SQLite project/session projections, Tauri commands/capabilities, UI, retained audio, OpenRouter, extra windows, and native packaging.
- Dependencies: P4-001 Project and portable-layout contracts; Manifest project/Markdown requirements; ADR 0001 Rust/Markdown ownership; ADR 0005 fixed-local workspace and handle-identity policy; existing workspace probe/reparse validation; existing `windows-sys`, Serde JSON, and tempfile dependencies.
- Acceptance criteria:
  - Re-probe and canonicalize the stored workspace, then hold a reparse-aware Windows directory handle that denies delete sharing. Record the handle's volume serial/file index and revalidate both that identity and the reparse-free path chain around every sensitive descendant create/open/write.
  - Accept only a validated P4-001 `Project`; derive `projects/<folderName>/project.md` internally; create no presets, sessions, database, journal, retained-audio, model, or other content artifact.
  - Render all Project fields in strict `schema_version: 1`/`document_type: project` YAML front matter. JSON-compatible quoted scalars and flow collections must preserve multiline/marker/tag/comment-like user text without creating another front-matter boundary or executable YAML structure.
  - Write a fixed same-directory temporary file with create-new semantics, sync its complete bytes, then publish through Windows atomic replacement with write-through. Duplicate project creation must return a fixed error without changing the existing snapshot.
  - Inject failures after directory pinning, temporary-file synchronization, and final publication; every case must remove the invocation's temporary/final file and newly created empty directories while retaining a pre-existing `projects/` directory. Reparse-backed descendants must leave their target untouched.
- Graphify evidence: exact-vocabulary expansion used `workspace`, `project`, `persistence`, `path`, `folder`, `security`, `write`, `markdown`, `atomic`, and `settings`. The stale graph confirmed the P2-003 workspace/settings-to-persistence seam but predates Phase 3/P4 code. Its recorded Python interpreter remains invalid on this Windows host, so reflection/save-result were unavailable; the accepted ADR, current source, and focused Windows tests are authoritative.
- Deliverables: `ProjectStore`, fixed path-free store errors and relative receipt, P4-001 project-only layout derivation, handle-based directory identity/revalidation, no-delete-share/reparse-aware directory opens, deterministic safe YAML rendering, same-directory synced Windows replacement, bounded ownership-aware cleanup, and a golden portable Markdown fixture. No dependency, lockfile, permission, command, event, database migration, frontend file, retained content, network path, or packaged asset changed.
- Verification evidence:
  - Eight focused Windows tests match the exact golden Markdown bytes, quote adversarial multiline/YAML-like scalars, create only the derived `project.md`, prevent workspace replacement while the store is alive, reject a junction-backed `projects/` path without touching its target, preserve an existing snapshot on duplicates, retain a pre-existing `projects/` directory during cleanup, and remove every owned artifact for faults after directory creation, temp sync, and publication.
  - Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 139 ordinary tests, and 3 capability tests; 12 explicit dependency/hardware/model/worker gates remain ignored in the ordinary suite.
  - Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed Prettier, ESLint, strict TypeScript, 19 Vitest files/98 tests, production build, and repository policy. Production npm audit found no known vulnerability; 159 frontend production licenses parsed; Cargo licenses/bans/sources passed; RustSec returned success with the existing 17 allowed maintenance/non-Windows warnings.
  - The locked x64 no-bundle release build completed, and the hidden executable remained alive and responsive for the bounded five-second launch smoke. Final diff, whitespace, scope, permission, dependency, lockfile, and artifact checks passed.
- Known limitations: P4-002 publishes new project snapshots only. It does not read or validate YAML from disk, update revisions, keep `.bak` recovery copies, detect external edits, list projects, rebuild SQLite, expose product commands/UI, create sessions, journal transcript mutations, recover interrupted meetings, persist the transient P3-015 run, retain audio, or call OpenRouter. Denying delete sharing deliberately prevents workspace/project-directory rename while the store is open, but Windows software with stronger privileges can still bypass ordinary sharing rules.

### P4-003 - Bounded project snapshot read, update, and recovery

- Status: completed 2026-08-11
- Owner: primary Codex agent
- Scope: make the P4-002 `project.md` boundary safely readable and revision-updateable, detect edits made after a read, retain one atomically produced backup, and recover a missing/malformed/oversized primary from that backup. Exclude workspace discovery, SQLite project projections, commands/capabilities/UI, sessions, transcript journals, retained audio, OpenRouter, extra windows, and native packaging.
- Dependencies: P4-002 pinned ProjectStore and canonical snapshot; P4-001 strict Project identity/revision contract; Manifest Markdown portability/resilience requirements; ADR 0001 Rust/Markdown ownership and conflict detection; ADR 0005 handle/reparse policy; ADR 0006 Rust-owned bounded front-matter validation; existing SHA-256, Serde JSON, `time`, and `windows-sys` APIs.
- Acceptance criteria:
  - Read at most 128 KiB through a leaf-reparse-aware handle that denies write sharing. Accept UTF-8 LF or CRLF only and parse the exact ordered version-one keys with JSON-compatible YAML scalar/flow values. Reject malformed, tagged, unknown, duplicate, reordered, non-UTF-8, trailing, oversized, or locator identity-mismatched content using fixed path-free errors.
  - Return the validated Project with a SHA-256 fingerprint of its exact source bytes. An update must match both `expectedRevision` and the fingerprint, keep ID/folder/creation identity immutable, increment revision exactly once, and never move `updatedAt` backwards.
  - Keep the primary file write-locked after validation and through the atomic Windows replacement so a concurrent editor cannot win between conflict detection and replacement. Sync the same-directory temporary file and retain the previous valid primary as `project.md.bak` through `ReplaceFileW`.
  - Recover a missing, malformed, or oversized primary only from a bounded valid identity-matching backup. Keep a malformed/oversized primary locked through replacement; use no-clobber publication for a missing primary; preserve the backup and re-read the restored primary before returning. Preserve unrecoverable input and remove a torn temp only when the primary is valid.
  - Faults after temp sync or replacement must return failure with the last acknowledged Project readable; the post-replacement fault must roll back from the valid backup.
- Graphify evidence: exact-vocabulary expansion used `project`, `markdown`, `revision`, `recovery`, `workspace`, `persistence`, `read`, `update`, and `path`. The stale graph connected settings revision handling, workspace validation, and Rust persistence but predates P4. Its recorded interpreter still cannot execute on this Windows host, so reflection/save-result were unavailable; current ADRs, source, and Windows tests are authoritative.
- Deliverables: validated `ProjectLocator`; exact-byte `ProjectSnapshotFingerprint`; bounded `ProjectSnapshot`; strict canonical front-matter parser; LF/CRLF support; immutable-identity/revision/time guards; write-denying snapshot handles with leaf reparse rejection; synced temp plus atomic backup replacement; missing-primary no-clobber restore; malformed/oversized locked restore; fault rollback and fixed errors. No dependency, lockfile, migration, command, event, capability, frontend file, retained content, network path, or packaged asset changed.
- Verification evidence:
  - Sixteen focused tests cover exact golden rendering, hostile scalar safety, LF/CRLF round-trip, unknown/duplicate/tag/document-type/trailing rejection, pinned creation/junction/duplicate behavior, successful revision update with exact old backup, fingerprint conflict, stale revision, immutable identity, skipped revision, missing/malformed/oversized recovery, unrecoverable preservation, torn-temp cleanup, and both injected update failures.
  - Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 147 ordinary tests, and 3 capability tests; 12 explicit dependency/hardware/model/worker gates remain ignored. Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed formatting, lint, strict types, 19 Vitest files/98 tests, production build, and repository policy.
  - Production npm audit found no known vulnerability; 159 frontend production licenses parsed; Cargo licenses/bans/sources passed; RustSec returned success with the existing 17 allowed maintenance/non-Windows warnings. The locked x64 no-bundle release build completed, and the hidden executable remained responsive for the five-second launch smoke.
- Known limitations: the parser deliberately accepts only KokoroKoe's canonical JSON-compatible YAML subset and exact key order; general YAML block collections, comments, aliases, tags, reordered keys, and Markdown body content are rejected. P4-003 requires a validated locator and does not scan/list projects, rebuild a SQLite projection, expose commands/UI, delete/export projects, create sessions, journal transcripts, recover live meetings, persist P3-015 output, retain audio, or call OpenRouter. Only the immediately previous valid project snapshot is retained as `.bak`.

### P4-004 - Bounded project discovery and rebuildable SQLite projection

- Status: completed 2026-08-11
- Owner: primary Codex agent
- Scope: add the Rust-owned bounded project discovery and rebuildable SQLite projection boundary over P4-003. Scan only direct P4 project folders, validate every accepted folder/document through the pinned store, surface invalid and recovered entries explicitly, and provide deterministic generation-bound pagination. Exclude product commands/capabilities/UI, sessions, transcript journals, retained audio, OpenRouter, extra windows, and native packaging.
- Dependencies: P4-003 strict project reads and backup recovery; P4-002 pinned workspace/project directories; P4-001 Project identity/order contract; Manifest Markdown-source/SQLite-index policy; ADRs 0001/0005/0006; existing rusqlite corruption handling and app-local data ownership.
- Acceptance criteria:
  - Enumerate at most 4,096 direct `projects/` entries without recursion. A missing directory is an empty catalog; an over-limit directory returns no partial result. Pin/revalidate the projects and candidate directories, reject reparse/non-directory/unsafe entries, and expose at most 256 fixed-code issues with only bounded control-free relative names.
  - Parse every accepted `project.md` through the P4-003 boundary, verify stored folder/full UUID identity, recover only from a valid matching backup, report recovered snapshots, and preserve invalid inputs. Exclude every candidate participating in a duplicate full project ID rather than choosing one arbitrarily.
  - Order accepted projects by parsed `updatedAt` descending, `createdAt` descending, then stable folder name. Pages accept limits 1–100 and carry opaque index-generation plus rank; malformed cursors fail before database access and a rebuild invalidates older cursors explicitly.
  - Store only rebuildable Project JSON, folder/UUID identity, exact snapshot SHA-256, sort rank, and generation in a dedicated app-local SQLite database. Replace the full projection in one immediate transaction so injected failures preserve the previous generation and rows.
  - Build an empty projection from Markdown on first page access. Quarantine a physically corrupt database/WAL family under an opaque UUID suffix and rebuild it solely from the discovered Markdown snapshots; never create a database or index file inside the portable workspace.
- Graphify evidence: actual-vocabulary expansion used `Phase 4 — Projects and Persistence`, `Project`, `Persistence ownership and recovery`, `Local Database and Index`, `settings_store.rs`, `map_sqlite_read_error()`, `open_connection_with_recovery()`, and `project-memory.md`. The stale graph traced the Manifest/architecture ownership rule and existing SQLite quarantine seam. Reflection failed because the configured Python executable is not a valid Windows application, so the current architecture, source, migration, and tests are authoritative.
- Deliverables: direct-child pinned discovery; bounded explicit discovery report; recovered-snapshot and duplicate-identity handling; stable timestamp/folder ordering; dedicated version-one project-index migration; transactional full rebuild with generation tracking; physical-corruption quarantine; auto-rebuild of an empty projection; opaque generation-bound pages; fixed path-free errors; and deterministic discovery/index/recovery/cursor/fault tests. No dependency, lockfile, command, event, capability, frontend file, workspace database, session, transcript, retained audio, network path, or packaged asset changed.
- Verification evidence:
  - Twenty-four focused project persistence tests cover strict snapshots plus direct non-recursive discovery, fail-closed entry/issue limits, explicit malformed/unexpected/recovered entries, duplicate-ID exclusion, Markdown-only empty/corrupt rebuild, stable multi-page order, stale cursor rejection, invalid page bounds, and rollback after injected delete/insert boundaries.
  - Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 155 ordinary tests, and 3 capability tests; 12 explicit dependency/hardware/model/worker gates remain ignored. Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed formatting, lint, strict types, 19 Vitest files/98 tests, production build, and repository policy.
  - Production npm audit found no known vulnerability; 159 frontend production licenses parsed; Cargo licenses/bans/sources passed; RustSec returned success with the existing 17 allowed maintenance/non-Windows warnings. The locked x64 no-bundle release build completed, and the hidden executable remained responsive for the five-second native launch smoke.
- Known limitations: P4-004 rebuilds the whole bounded project projection and does not yet incrementally refresh one row, expose `list_projects`/`get_project`/create/update commands, render project UI, delete/export projects, create or index sessions, add FTS, journal transcripts, recover live meetings, persist P3-015 output, retain audio, or call OpenRouter. Corruption recovery is proven for physical SQLite corruption; a future schema fails closed and requires a compatible application version rather than destructive downgrade.

### P4-005 - Product project-management boundary

- Status: completed 2026-08-11
- Owner: primary Codex agent
- Scope: expose P4-004/P4-003 project management to the exact `main` WebView through strict list/get/create/update commands and a bounded Home list/create/edit surface. Preserve Markdown authority, optimistic revision/fingerprint conflicts, and app-local SQLite as a rebuildable projection. Exclude delete/export, sessions, transcript journals, retained audio, OpenRouter, extra windows, and native packaging.
- Dependencies: P4-004 bounded generation-indexed catalog; P4-003 exact-byte fingerprint and revision-checked update; P4-002 pinned atomic project snapshot writer; P4-001 Project contract; settings workspace service; exact-main authorization and sanitized-error patterns; React Query and strict Rust/Zod transport patterns.
- Acceptance criteria:
  - Share strict version-one Rust/Zod contracts for `PageRequest`, `ProjectPage`, `CreateProjectInput`, and nonempty partial `UpdateProjectInput`. Reject null, unknown, malformed, duplicate, unbounded, unsafe, and cross-field-inconsistent values on both sides of Tauri.
  - Expose only `list_projects`, `get_project`, `create_project`, and `update_project`. Authorize the exact `main` WebView before service access, run filesystem/SQLite work on blocking threads, and return only fixed sanitized errors with no workspace path, snapshot bytes, fingerprint, or index identity.
  - Serialize workspace selection/settings mutation and project operations. Bind the shared app-local projection to a SHA-256 identity of the canonical workspace so a workspace switch cannot return another workspace's projects; migrate schema v1 to v2 without storing the raw workspace path.
  - Before a create/update Markdown mutation, transactionally invalidate the current index generation. A successful command requires a complete Markdown-only rebuild containing the written project. If the durable Markdown write succeeds but projection refresh fails, return `project_projection_refresh_pending`; the next read/list rebuilds before returning data.
  - Preserve immutable project identity and creation time. Create generates UUID/folder/revision in Rust; update requires displayed revision, rereads the exact snapshot/fingerprint, increments once, and never overwrites an external or concurrent edit.
  - The Home UI fetches at most 24 projects per page, renders inert project text, loads further pages explicitly, creates with current local preset/model defaults, and submits edits with the displayed revision. React receives no path, fingerprint, Markdown, filesystem, shell, process, HTTP, or generic Tauri capability.
- Graphify evidence: query expansion used the actual graph vocabulary `project`, `command`, `capability`, `home`, `react`, `contract`, `update`, `create`, `revision`, `tauri`, `settings`, and `window`. The stale graph linked exact-main settings commands, domain contracts, authorization, and project-memory seams. Reflection remained unavailable because the configured `python.exe` is not a valid Windows application, so the current Manifest, architecture, source, migrations, and tests were authoritative.
- Deliverables: strict shared project-management fixture; Rust create/update/page domain logic; workspace-serialized `ProjectService`; four exact-main blocking commands; fixed sanitized project errors; four generated command permissions and minimal main capability grants; workspace-bound project-index schema v2; pre-write generation invalidation and post-write verified rebuild; pending-refresh repair; strict React adapters and query hooks; 24-item Home pagination; local create/edit form; and deterministic contract, authorization, durability, conflict, workspace-switch, migration, refresh-fault, adapter, router, and UI tests.
- Verification evidence:
  - Thirty-six focused Rust project tests cover shared contracts, UUID/folder creation, partial update validation, exact-main authorization, pinned snapshot persistence, create/list/get/update/restart behavior, stale revision rejection, workspace switching, v1-to-v2 index migration, and durable-write/pending-refresh repair.
  - Rust 1.88 `pnpm verify:rust` passed locked x64 rustfmt, Clippy with warnings denied, 163 ordinary tests, and 3 capability tests; 12 explicit dependency/hardware/model/worker gates remain ignored. Node 24.19.0/pnpm 10.30.2 `pnpm verify:frontend` passed formatting, lint, strict TypeScript, 21 Vitest files/116 tests, production build, and repository policy.
  - Production npm audit found no known vulnerability; 159 frontend production licenses parsed; Cargo licenses/bans/sources passed; RustSec returned success with the existing 17 allowed maintenance/non-Windows warnings. The locked x64 no-bundle release build completed, and the hidden executable remained alive and responsive for the five-second launch smoke.
- Known limitations: projection refresh currently performs a complete bounded catalog rebuild after every acknowledged project write rather than an incremental row refresh. The Home surface edits only project metadata and has no project-detail route. Delete/export, presets within project folders, session creation/indexing, session Markdown, transcript journals/search/recovery, persistence of the P3-015 live transcript, retained audio, OpenRouter, extra windows, and native runtime/installer packaging remain out of scope. Native visual automation was not required for this bounded form/list surface; behavior is covered by React interaction tests and the release launch smoke.

## Known environment facts

- The machine-wide shell still defaults to Node 26.5.1; P2 verification initializes `fnm` and uses the repository-pinned Node 24.19.0.
- pnpm 10.30.2, Cargo 1.88.0, and Rust 1.88.0 are installed.
- Visual Studio 2022 Build Tools 17.14, Windows SDK 10.0.26100.0, and CMake 4.4.1 are installed and verified.
- Vulkan 1.4 is available with an NVIDIA GeForce RTX 3070 Ti. LunarG Vulkan SDK 1.4.350.0 is copied under the external P3-005 user-local scratch directory without registry or persistent `PATH` changes.
- CPU-only and non-Vulkan Windows machines remain required test targets.

## Next handoff

Define P4-006 as the Rust-owned initial session persistence boundary inside an existing project: create one immutable-identity session folder and strict `session.md` snapshot from validated project/preset/device/model/privacy inputs, using the P4 pinned-workspace and atomic-publication rules. Keep transcript journals, live-capture wiring, session commands/UI/indexing, retained audio bytes, OpenRouter, extra windows, and native packaging as later bounded tasks.
